以下为对本仓（CI-Workflows）的静态检查中，可被本仓文件与线上 GitHub API 客观证明的事实。仅陈述行为与证据位置，不含处置建议。

## A. `v1` 大版本指针可被强移，且本仓被业务仓以 `@v1` 引用
- README.md「版本策略」原文：`git tag v1.0.0 && git push --tags`，然后 `git tag -f v1 v1.0.0 && git push -f origin v1` 移动指针。
- 线上 `release-tags` ruleset（id 20968896，target=tag，enforcement=active）：`bypass_actors = [OrganizationAdmin always]`，规则含 `update` / `non_fast_forward` / `deletion`。
- template-service/.github/workflows/ci.yml 的 `hygiene` / `check` / `deps` 均以 `Cloudbird-Software/CI-Workflows/.github/workflows/<name>.yml@v1` 引用。
- API 实测：`refs/tags/v1` 指向 `8cccf1b2abc7f5ce835b4d5c3455aa28968effd3`，与 `v1.2.0` 相同（浮动指针）。
→ 事实组合：持有 admin 权限者可强制移动 `v1` 指针，从而改变所有业务仓实际执行的 CI 内容。

## B. zizmor.yml 显式忽略本仓 ci.yml 的 unpinned-uses（自引用 @main 不 pin SHA）
- zizmor.yml 全文注释：`本仓 ci.yml 自引用 ./.github/workflows/hygiene.yml@main（随 main 演进），不 pin SHA 是有意为之`。
- zizmor.yml `rules.unpinned-uses.ignore = [ci.yml]`。
→ 事实：本仓自身对工作流的引用不固定 SHA。

## C. 全部工作流未声明 `timeout-minutes`，与测试政策声明不一致
- check.yml / hygiene.yml / dep-review.yml / release.yml / ci.yml 全文均无 `timeout-minutes`。
- 治理仓（.github）`governance/policy/testing.yaml` 声明原则：`gate < 5min`。
→ 事实：实现与政策声明不一致，gate 卡死无熔断上限。

## D. 本仓工作流无 CodeQL 扫描步骤，而线上 `codeql-gate` ruleset 要求 CodeQL 结果
- 本仓 `.github/workflows` 下仅 check / ci / dep-review / hygiene / release / scorecard 六个 workflow，均无 `codeql-action/analyze` 或 code-scanning 上传步骤。
- 线上 `codeql-gate` ruleset（id 20970889，target=branch，enforcement=active）：要求 CodeQL，`security_alerts_threshold=medium_or_higher`、`alerts_threshold=errors`。
→ 事实：本仓 CI 无 CodeQL 扫描实现，而该 ruleset 对默认分支强制 CodeQL 结果。
