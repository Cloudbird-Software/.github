以下为对本仓（template-service）的静态检查中，可被本仓文件与线上 GitHub API 客观证明的事实。仅陈述行为与证据位置，不含处置建议。

## A. CODEOWNERS 全部路径唯一归属 `@randypanding`（单点），且 ruleset 要求 code owner 审查与 0 个批准并存
- `.github/CODEOWNERS`：`* @randypanding`（全部路径唯一 code owner）；注释自述 `App（cloudbrid-agent）不在任何 owner 列表 = 永不满足 code owner 审查`。
- 线上 `main-protection` ruleset（id 20968893，target=branch，enforcement=active）：`require_code_owner_review: true` 且 `required_approving_review_count: 0`。
→ 事实：所有路径的唯一 code owner 为同一账号；该 ruleset 同时要求 code owner 审查又要求 0 个批准。

## B. dependabot 自动合并与"依赖需审批"政策并存
- `.github/workflows/automerge.yml`：`on: pull_request_target`；对 dependabot 非 major PR 执行 `gh pr merge --auto --squash`（使用 App 令牌 `AGENT_APP_SECRET`，permission-contents: write、permission-pull-requests: write）。
- `.github/dependabot.yml`：npm `minor-patch` 分组自动更新（`update-types: [minor, patch]`）。
- 治理仓（.github）`governance/policy/languages.yaml`：`dependency_policy.approval_required: true`。
→ 事实：依赖 minor/patch 变更可在无人工审批的情况下被自动合并，与 `approval_required` 声明并存。

## C. `deps` 检查仅 PR 触发，直接 push 到 main 时被跳过而 gate 仍判绿
- `.github/workflows/ci.yml`：`deps` job 条件 `if: github.event_name == 'pull_request'`；`gate` 对 `needs` 中 result != success 且 != skipped 的 job 才报错（skipped 视为通过）。
- 线上 `main-protection` ruleset 的唯一 required status check 为 `gate`。
→ 事实：push 到 main 的提交不运行 `deps`（依赖审查被跳过），gate 仍可通过。

## D. 本仓工作流无 CodeQL 步骤，而线上 `codeql-gate` ruleset 要求 CodeQL
- 本仓 `.github/workflows` 下仅 ci / automerge / scorecard 三个 workflow，均无 CodeQL 扫描步骤。
- 线上 `codeql-gate` ruleset（id 20970889，target=branch，enforcement=active）：要求 CodeQL，`security_alerts_threshold=medium_or_higher`、`alerts_threshold=errors`。
→ 事实：本仓 CI 无 CodeQL 实现，而 ruleset 对默认分支强制 CodeQL 结果。
