# Generated by linkml-typedb-generator v0.1.0
# Schema: dga-ontology
# https://linkml.io

define

  # Attribute types
  attribute boundary, value string;
  attribute id, value string;
  attribute name, value string;
  attribute created-at, value datetime;
  attribute full-name, value string;
  attribute org-role, value string;
  attribute contact-channel, value string;
  attribute statement, value string;
  attribute closure-type, value string;
  attribute desired-outcome, value string;
  attribute source-ref, value string;
  attribute uri, value string;
  attribute version, value string;
  attribute expiry-condition, value string;
  attribute authority-source, value string;
  attribute model-refs, value string;
  attribute endpoint-ref, value string;
  attribute capability-version, value string;
  attribute content-digest, value string;
  attribute object-version, value string;
  attribute storage-uri, value string;
  attribute object-locked, value boolean;
  attribute trace-ref, value string;
  attribute judge-model, value string;
  attribute judge-version, value string;
  attribute calibration-domain-ref, value string;
  attribute model-set-snapshot, value string;
  attribute spec-version, value string;
  attribute harness-version, value string;
  attribute observation-window-start, value datetime;
  attribute observation-window-end, value datetime;
  attribute ontology-commit, value string;
  attribute validity-status, value boolean;
  attribute currency, value string;
  attribute amount-cap, value double;
  attribute period, value string;
  attribute retired-at, value datetime;
  attribute promise, value string;
  attribute promisee, value string;
  attribute linked-authorization, value string;
  attribute scope, value string;
  attribute decision-timespan, value string;
  attribute granted-executor, value string;
  attribute valid-from, value datetime;
  attribute valid-until, value datetime;
  attribute option-label, value string;
  attribute tradeoff, value string;
  attribute decision-object, value string;
  attribute evidence-refs, value string;
  attribute recommendation, value string;
  attribute deadline, value datetime;
  attribute approval-consequences, value string;
  attribute decided-at, value datetime;
  attribute start-date, value datetime;
  attribute end-date, value datetime;
  attribute deliverable-spec, value string;
  attribute linked-service, value string;
  attribute service-scope, value string;
  attribute sla-summary, value string;
  attribute related-projects, value string;
  attribute params-schema-ref, value string;
  attribute preconditions, value string;
  attribute side-effects, value string;
  attribute permission-predicate-ref, value string;
  attribute idempotency-key, value string;
  attribute retry-owner, value string;
  attribute executor-binding, value string;
  attribute input-schema-ref, value string;
  attribute output-schema-ref, value string;
  attribute hidden-sealed-ref, value string;
  attribute authorization-ref, value string;
  attribute git-binding-ref, value string;
  attribute responsibility-scope, value string;
  attribute delegated-executor, value string;
  attribute assumed-at, value datetime;
  attribute parent-closure, value string;
  attribute child-closure, value string;
  attribute nesting-rationale, value string;
  attribute timespan-constraint-checked, value boolean;
  attribute authority-inheritance-declared, value boolean;
  attribute closure, value string;
  attribute filled-at, value datetime;
  attribute original-ref, value string;
  attribute projection-ref, value string;
  attribute clipping-declaration, value string;
  attribute transform-pipeline, value string;
  attribute writeback-allowed, value boolean;
  attribute cross-read-allowed, value boolean;
  attribute rpm-limit, value integer;
  attribute tpm-limit, value integer;
  attribute daily-quota, value double;
  attribute monthly-quota, value double;
  attribute credit-balance, value double;
  attribute observed-at, value datetime;
  attribute availability-pct, value double;
  attribute latency-ms-p95, value integer;
  attribute error-rate, value double;
  attribute last-failure-at, value datetime;
  attribute last-check-at, value datetime;
  attribute tos-risk-matrix-ref, value string;
  attribute data-usage-terms-summary, value string;
  attribute notes, value string;
  attribute provider-attr, value string;
  attribute credential-ref, value string;
  attribute endpoint-name, value string;
  attribute bound-accounts, value string;
  attribute routing-policy-ref, value string;
  attribute target-capability, value string;
  attribute quota-constraints, value string;
  attribute fallback-chain, value string;
  attribute virtual-key-isolated, value boolean;
  attribute suspension-on-timespan-expiry, value boolean;
  attribute approval-ref, value string;
  # Enum: trustlevel (permitted values: T0, T1, T2)
  attribute trust-level, value string;
  # Enum: dataclass (permitted values: D_PUBLIC, D_INTERNAL, D_CUSTOMER_SENSITIVE)
  attribute data-class, value string;
  # Enum: knowledgeform (permitted values: SPEC, MEMORY, NEGATIVE_RESULT, CAPABILITY_FRONTIER)
  attribute asset-type, value string;
  # Enum: executortype (permitted values: AGENT, TOOL, MODEL, PIPELINE)
  attribute executor-type, value string;
  # Enum: evidencetype (permitted values: EXECUTION_RECEIPT, ACCEPTANCE_VERDICT, AUTHORIZATION_RECORD, CALIBRATION_ASSET, POLICY_DECISION)
  attribute evidence-type, value string;
  # Enum: closurestate (permitted values: DRAFT, REGISTERED, AUTHORIZED, RUNNING, AWAITING_VERIFICATION, SUSPENDED, COMPLETED, FAILED, RETIRED)
  attribute state, value string;
  # Enum: autonomylevel (permitted values: L0, L1, L2, L3, L4, L5)
  attribute autonomy-level, value string;
  # Enum: evidencetype (permitted values: EXECUTION_RECEIPT, ACCEPTANCE_VERDICT, AUTHORIZATION_RECORD, CALIBRATION_ASSET, POLICY_DECISION)
  attribute required-evidence-type, value string;
  # Enum: failuresemantics (permitted values: RETRY_SAFE, COMPENSATE, MARK_UNVERIFIED)
  attribute failure-semantics, value string;
  # Enum: functionkind (permitted values: EVALUATION, CALIBRATION, DRIFT_DETECTION, METRIC)
  attribute function-kind, value string;
  # Enum: roletype (permitted values: EXECUTION, COORDINATION, CONTROL, AUDIT, INTELLIGENCE, IDENTITY, INTENT_HOLDER, CONTEXT_PROVIDER, VERIFIER, ACCOUNTABLE)
  attribute role-attr, value string;
  # Enum: gradientzone (permitted values: TRUTH_ZONE, WORKSPACE, REDUNDANCY_ZONE)
  attribute gradient-zone, value string;
  # Enum: costtier (permitted values: FREE, LOW_PAID, HIGH_PAID, ENTERPRISE)
  attribute cost-tier, value string;
  # Enum: trustlevel (permitted values: T0, T1, T2)
  attribute trust-label, value string;
  # Enum: tosrisklevel (permitted values: CLEAR, GREY_ZONE, HIGH_RISK)
  attribute tos-risk, value string;
  # Enum: costtier (permitted values: FREE, LOW_PAID, HIGH_PAID, ENTERPRISE)
  attribute allowed-cost-tiers, value string;
  # Enum: trustlevel (permitted values: T0, T1, T2)
  attribute min-trust-level, value string;
  # Enum: sensitivitylevel (permitted values: S0_PUBLIC, S1_INTERNAL, S2_CUSTOMER_SENSITIVE)
  attribute max-data-sensitivity, value string;

  # Entity types
  entity securitymark,
      owns trust-level,
      owns data-class,
      owns boundary;
  
  entity person,
      owns id @key,
      owns name,
      owns created-at;
  
  entity intent,
      owns id @key,
      owns name,
      owns created-at;
  
  entity contextasset,
      owns id @key,
      owns name,
      owns created-at;
  
  entity capabilityentity,
      owns id @key,
      owns name,
      owns created-at;
  
  entity evidenceentity,
      owns id @key,
      owns name,
      owns created-at;
  
  entity calibrationdomain,
      owns id @key,
      owns name,
      owns created-at;
  
  entity budget;
  
  entity commitment,
      owns id @key,
      owns name,
      owns created-at,
      owns retired-at;
  
  entity authorization,
      owns id @key,
      owns name,
      owns created-at;
  
  entity decisionoption;
  
  entity s5decisioncard,
      owns id @key,
      owns name,
      owns created-at;
  
  entity project,
      owns id @key,
      owns name,
      owns created-at,
      owns retired-at;
  
  entity service,
      owns id @key,
      owns name,
      owns created-at,
      owns retired-at;
  
  entity actiontype,
      owns id @key,
      owns name,
      owns created-at;
  
  entity functiontype,
      owns id @key,
      owns name,
      owns created-at;
  
  entity closureunit,
      owns id @key,
      owns name,
      owns created-at;
  
  entity accountabilitylink,
      owns id @key,
      owns name,
      owns created-at;
  
  entity closurenesting,
      owns id @key,
      owns name,
      owns created-at;
  
  entity roleslot,
      owns id @key,
      owns name,
      owns created-at;
  
  entity originalprojection,
      owns id @key,
      owns name,
      owns created-at;
  
  entity quotastate;
  
  entity healthsnapshot;
  
  entity provider,
      owns id @key,
      owns name,
      owns created-at,
      owns retired-at;
  
  entity poolaccount,
      owns id @key,
      owns name,
      owns created-at,
      owns retired-at;
  
  entity logicalendpoint,
      owns id @key,
      owns name,
      owns created-at,
      owns retired-at;
  
  entity routingpolicy,
      owns id @key,
      owns name,
      owns created-at;
  

