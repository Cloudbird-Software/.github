# BUILD-LOG · DGA-INFRA 本机建设日志（append-only，证据件）

格式：每条 = 时间(UTC) | 轨道 | 事件 | 证据指针

- 2026-09-06 ~18:3x | PM | 环境侦察：无 Docker、无 WSL 发行版；Python 3.11.9+pip 26.2.1、Node 24.19+npm 11.17、winget 1.9+choco 2.7.3；端口 5432/7233/8233/8181/8200/6363/4000/8090 全空闲；D 盘 413G 空闲 | 本条
- 2026-09-06 ~18:3x | PM | 工作区落位 D:/Projects/dga-infra（gov-infra-repo 第 4 章结构 + services/）；权威文档归档：DGA-INFRA-v1.0.md（S5 原文逐字）、ontology-research-2026-09-06.md（本体论研究+裁决）、S5-PENDING-DEFAULTS.md（第 9 章默认方案 8 项+追加 A/B 两项） | docs/
- 2026-09-06 ~18:3x | PM | 阻塞判定 B：TerminusDB/TypeDB 无 Windows 原生构建且无 Docker/WSL——名词运行时本机以 PG+适配器接口先行（登记 S5-PENDING-DEFAULTS 追加项 B） | S5-PENDING-DEFAULTS.md
- 2026-09-06 ~23:4x | ENV | OPA 1.20.2 落位本机原生运行（注：仓库实际 org=open-policy-agent，任务给的 openpolicyagent 404，已换正名 URL）：127.0.0.1:8181，smoke.rego（package dga.smoke，Rego v1 语法 allow if）两态判定 true/false 验证通过 | services/opa/HEALTH.txt
- 2026-09-06 ~23:4x | ENV | Temporal CLI 1.8.3（Server 1.31.2/UI 2.50.1）start-dev 落位：gRPC 7233 / UI 8233，sqlite 持久化 data/temporal-dev.db；operator cluster describe=active、UI HTTP 200 | services/temporal/HEALTH.txt
- 2026-09-06 ~23:4x | ENV | OpenBao v2.6.2 有 Windows 原生构建（任务预判"可能无"不成立）：bao server -dev 落位 127.0.0.1:8200，bao status=unsealed、kv put/get secret/dga/smoke 往返通过；root token 记 services/openbao/dev-root-token.txt（dev inmem 临时，重启即换） | services/openbao/HEALTH.txt
- 2026-09-06 ~23:4x | ENV | TerminusDB 阻塞确认登记（无 Windows 原生构建+本机无 Docker/WSL，按 PM 预判不尝试）| B 判定见 docs/S5-PENDING-DEFAULTS.md + 本条
- 2026-09-06 ~23:5x | 本体论轨道 M0 | 任务一完成：venv 建于 .venv（Python 3.11.9）；安装 linkml 1.11.1 + linkml-runtime 1.11.1 + jsonschema 4.26.0（官方源一次成功，清华源兜底未启用） | .venv/
- 2026-09-06 ~23:5x | 本体论轨道 M0 | 任务二完成：dga-ontology.yaml 主 schema + include/ 七模块（enums/base/core/actions/functions/closure/pools，import 无环）；27 类（26 具体 + SecurityMark mixin，覆盖五层）、14 枚举、5 自定义类型、150 槽位；ID 前缀体系 22 前缀定稿（CU/INT/CTX/CAP/ACT/FN/EVD/CMT/AUT/DC/PSN/CAL/PRJ/SVC/PRV/ACC/EPL/RTP/ALK/CNL/RSL/OPR，全局 pattern + 各类 slot_usage 收紧）；R-GOV-03 锚点=ClosureUnit.accountability required→AccountabilityLink.responsible_person 类型钉死 Person；R-GOV-04 锚点=S5DecisionCard 六字段全 required；R-POOL-CALIB/R-EVID-04 锚点=CalibrationDomain（模型集合快照+spec/harness 版本+观察窗+ontology_commit）；R-GOV-05=Project/Service 分离登记；INFRA 3.1 全字段=RoutingPolicy 五约束+降级链/预算/虚拟key/时距挂起 | schemas/dga-ontology.yaml, schemas/include/
- 2026-09-06 ~23:5x | 本体论轨道 M0 | SEL-13b 生成器验证完成：gen-json-schema(134,413B，$defs 41)、gen-python(145,099B，27 dataclass，ast 编译通过)、gen-sqlddl(66,995B，30+表，ClosureUnit.accountability_id TEXT NOT NULL+FK)、gen-doc(213 文件)、gen-typedb(9,765B——本版存在，自报生成器 v0.1.0)；工具链=linkml 1.11.1；坑两处已修：①模块内 import 相对自身目录解析（include/include/ 报错→改兄弟名导入）②permissible_value 的 text 必须等于键名（中文显示名改放 title 字段）；gen-python 需 importmap.json（其 import 按 CWD 解析，其余生成器按 schema 目录解析） | schemas/generated/
- 2026-09-06 ~23:5x | 本体论轨道 M0 | 任务四完成：负面测试四案例全符合预期——①缺 accountability 的 ClosureUnit 被拒（'accountability' is a required property，R-GOV-03）②T0×客户敏感路由：schema 字段在位通过，跨对象不等式谓词诚实登记为 OPA 层职责（附 rego 草案，真实拒绝验收在 M2/M4）③trust_label=T9 被拒（enum T0/T1/T2）④阳性对照通过；实际输出逐字贴入 md，复跑退出码 0 | controls/negtest-m0.md, controls/negtest_m0.py
- 2026-09-06 ~23:5x | 本体论轨道 M0 | 遗留缺口登记：①FAIR URI 基域暂用 example.org 占位，正式域名待 S5 定夺；②枚举取值键为 ASCII（中文显示名在 title 字段供人类投影），若 S5 要求数据面直接用中文值需改键并重生成；③ClosureNesting"子时距≤父"与 R-POL-02 权限继承以注释+核验标记位登记，谓词强制在 OPA 层；④TerminusDB/TypeDB 运行时落位随服务器部署阶段（追加项 B），gen-typedb 产物已就位待加载验证；⑤gen-terminusdb 同版存在未纳入本轮验收（登记不阻塞） | 本条 + controls/negtest-m0.md
- 2026-09-06 ~23:5x | ENV | PostgreSQL 16.9-1（EDB binaries zip 314,500,604B）落位：initdb(locale C, UTF8, user=dga, trust)→pg_ctl start :5432→建库 dga_control；SELECT version() 证据 GREEN；curl 断点续传(-C -)补齐超时中断的下载 | services/postgres/HEALTH.txt
- 2026-09-06 ~23:5x | ENV | services/ 全组件运维脚本落位：每组件 start.sh/stop.sh/health.sh（幂等：端口已监听跳过；health 刷新 HEALTH.txt 并回 GREEN/RED 退出码）+ services/README.md 汇总表 | services/README.md
- 2026-09-06 | SEL | SEL-02 Temporal 报告交付：SQL(Advanced Visibility) 为正式支持形态(MySQL≥8.0.17/PG≥12@Server≥1.20)、ES 仅为"多于少量执行推荐"、Cassandra visibility 已移除(v1.24)、Server v1.31.2/CLI v1.8.3 含 Windows 资产；默认=生产 PG 单库+本机 dev server | catalog/sel/SEL-02.md
- 2026-09-06 | SEL | SEL-04 OpenJiuwen 报告交付：项目已定位(GitHub openJiuwen-ai, 18 仓, jiuwenswarm 8133 stars, Apache-2.0 逐字核验, 当日有 push)；deepsearch 无 LICENSE 禁用；定位仍待 S5 | catalog/sel/SEL-04.md
- 2026-09-06 | SEL | SEL-05 LiteLLM 报告交付：LICENSE=MIT(Berri AI 2023)+enterprise/ 目录 carve-out 逐字核验；OSS 官方明列 virtual keys/spend/budgets/fallbacks；key 轮换/SSO>5人/审计日志=Enterprise；Redis 仅多实例必需；默认=OSS 自托管单实例+PG，企业版不采购 | catalog/sel/SEL-05.md
- 2026-09-06 | SEL | SEL-06 凭据库报告交付：OpenBao MPL-2.0(v2.6.2 含 windows_amd64/arm64 资产逐枚举) vs Vault BUSL-1.1(IBM, Additional Use Grant/竞争条款/4年转MPL 全文核验, 2.1.0 windows_amd64 在售)；默认=OpenBao+本机 Raft 单节点 | catalog/sel/SEL-06.md
- 2026-09-06 | SEL | SEL-07 对象存储报告交付：B2=完整 S3 Object Lock(governance/compliance 1-3000天)且桶天然版本化、出口 3x 免费；R2=bucket locks 专有 API 非标；MinIO 社区版停发预编译二进制(release 资产=0, AGPL, 源码分发)移出默认候选；S3=语义基准(页面定价 JS 未全渲染已标注)；默认=主选 B2 | catalog/sel/SEL-07.md
- 2026-09-06 | SEL | SEL-08 Langfuse 报告交付：LICENSE 核心目录 MIT Expat + ee|web/src/ee|worker/src/ee =企业许可(版权行=ClickHouse, Inc., 2026-01-16 被收购)；自托管必限 Postgres+ClickHouse(≥24.3)+Redis+Blob 容器栈、ClickHouse 安装路径无 Windows；结论=本机不可自托管，默认=本机仅 OTel 管道、Langfuse 推迟 M6 服务器阶段 | catalog/sel/SEL-08.md
- 2026-09-07 ~00:4x | 控制面轨道 M1 | SEL-01 控制面技术栈交付（可逆实现选择，agent 依 8.1 记录理由）：Python 3.11.9 + FastAPI 0.141.1 + uvicorn 0.52.4 + psycopg3 3.3.5 同步薄层（.venv 实装 pip list 为证）；理由=六元组 schema 直映射（API 字段=LinkML 槽位名，pydantic 经 gen-json-schema 对齐）、单进程部署简单、一人+agent 形态；回滚成本=换框架只动 api.py（db.py 唯一 SQL 触点不动）| catalog/sel/SEL-01.md
- 2026-09-07 ~00:4x | 控制面轨道 M1 | 建库完成：gen-sqlddl 产物（38 CREATE TABLE）加载 dga_control + 补丁后共 43 表（38 本体 CamelCase + 4 先期池表 + 1 cp_decision_addressee）；PG 语法补丁三处登记——①DATETIME→TIMESTAMP×39（PG 无 datetime 类型）②删 ClosureUnit 块两条前向 FK（Intent/AccountabilityLink 未建表，PG 即时解析 FK 报错）及其尾逗号③补丁 SQL 以 DEFERRABLE INITIALLY DEFERRED 重建 ClosureUnit↔AccountabilityLink 环（环插法前提，NOT NULL 不延迟）；ClosureUnit.accountability_id NOT NULL+FK 实测在位 | catalog/sel/SEL-01-ddl-patch.sql
- 2026-09-07 ~00:4x | 控制面轨道 M1 | 控制面 v0 服务上线 :8090：POST/GET /closures（六元组登记，环插法+三分量挂接）、POST /decisions + GET /decisions/queue + GET /decisions/{id} + POST /decisions/{id}/resolve（R-GOV-04 六字段+addressee；X-S5-Person≠addressee→403，R-SEC-04 最小形态）、POST/GET /authorizations（scope/budget/decision_timespan 在位，R-POL-02）、GET /healthz（DB+43 表+PG 版本）；curl 十案例全绿；cp_decision_addressee=控制面自有表（本体无 addressee 槽位，加槽待 S5）| control-plane/（app/main.py+db.py+api.py+README）
- 2026-09-07 ~00:4x | 控制面轨道 M1 | 运维脚本落位（幂等）：services/scripts/start-control-plane.sh（pid/端口双守卫，uvicorn 后台，run.log+HEALTH.txt 就绪等待）/stop-control-plane.sh（netstat 取 Windows pid taskkill，MSYS/Windows pid 分离处理）；start→stop→stop→start 全周期实测幂等 | services/scripts/、services/control-plane/run.log
- 2026-09-07 ~00:4x | 控制面轨道 M1 | M1 负面测试四案例+阳性对照全绿（可复跑，连跑两轮 exit 0）：①缺 accountability→422 且 ClosureUnit 行数不变（1→1，R-GOV-03 API+DB 双层）②决策卡缺字段 7/7→422（R-GOV-04 六字段+addressee 逐字段）③resolve 错人/缺头→403×2 且卡片保持 pending，正确人 200、重复 409（R-SEC-04）④合法闭环 201+GET 200+DB +1；实际 curl+响应逐字贴 md | controls/negtest-m1.md, controls/negtest_m1.sh
- 2026-09-07 ~00:4x | 控制面轨道 M1 | 飞书四工作台占位件（诚实边界：无凭据不集成不伪造）：W1 闭环登记/W2 审批/W3 S5 队列/W4 资产目录 ↔ 控制面 API 字段映射表（映射源=LinkML schema）+ 接入前置条件六项（app token/OpenBao 落点、权限 scope 最小集、user_id↔PSN 对照表、回调入口、写路径纪律、W2 合规性待 S5）| connectors/feishu/README.md
- 2026-09-07 ~00:4x | 控制面轨道 M1 | 遗留缺口登记：①飞书真实集成待凭据与服务器阶段；②X-S5-Person 头=身份占位，本机单用户可接受、不得延伸生产（真实身份体系 R-SEC-01/02 待 M6+）；③master data（Person/Intent/Capability 等）v0 经 SQL 幂等 seed，管理界面随工作台阶段；④谓词级权限/预算强制在 OPA+工具网关（M4），本服务只保授权记录权威性；⑤cp_decision_addressee 待本体加槽后迁移（S5 裁决点）；⑥gen-sqlddl 产物的 PG 兼容性问题已登记补丁文件，建议上游生成器修（DATETIME/前向 FK），索引名超 63 字节截断 1 处 NOTICE 不阻塞 | 本条
- 2026-09-07 ~01:0x | 资源池轨道 M2 | 任务1 池账本落位：dga_control 建 pool_providers/pool_accounts/pool_routing_policies/pool_endpoints 四表（pools/ledger.sql，幂等 DDL+种子）。本体生成物登记：schemas/generated/dga-ontology.sql 虽含 PoolAccount/LogicalEndpoint/RoutingPolicy 生成表但不复用——DATETIME 列 PG16 不可装载、quota/health 被拆标量表、无降级链数组；pool_* 表字段与本体类一一对应（PRV/ACC/EPL/RTP 前缀不变）。种子全 mock 零真实密钥：PRV-0001 mock-freelayer(T0 依据)+PRV-0002 mock-paidlayer(T1 依据) 均 mock_response 模式；ACC-0001 free/T0/clear、ACC-0002 low-paid/T1/clear、ACC-0003 free/T0/grey-zone（3.4 灰区场景，DB 约束 grey_zone_forced_t0 钉死灰区必 T0）；EPL-0001 free-workhorse(T0)+EPL-0002 paid-judge(T1)（任务种子点名两端口）+EPL-0003 gray-probe（负面测试③所需新增，隔离端点，已登记）；RTP-0001 成本白名单[free,low-paid]+fallback_chain 数组+budget_cap+virtual_key_isolated=true。credential_ref=openbao://kv/dga/pool/<alias>（SEL-06 格式；OpenBao dev inmem 未落 secret——缺口登记）。DB 约束在位：ID pattern/cost_tier/trust_label 枚举/grey_zone_forced_t0 | pools/ledger.sql
- 2026-09-07 ~01:0x | 资源池轨道 M2 | 任务2 OPA 池路由谓词：policies/pool-routing.rego（package dga.pool，Rego v1）。语义=3.2 表 rank(sensitivity)≤rank(trust)（public/internal/customer_sensitive↔T0/T1/T2）+3.3 红线显名（customer_sensitive×T0 额外 R-POOL-DATA 拒因）+3.4 灰区规则（tos_risk∈{grey-zone,high-risk}×非公开→deny，独立于信任级）+未知枚举拒（R-POOL-INPUT-INVALID，不默认放行）。查询面 /v1/data/dga/pool/{allow,deny_reasons,decision}。加载方式：OPA 1.20.2 多文件/目录参数，services/opa/start.sh 最小改动两行（追加 POLICY_DIR=policies/ 目录）；smoke.rego 回归通过。10 项谓词语义实测全部符合设计（含灰区误标 T1 仍 deny） | services/opa/start.sh, policies/pool-routing.rego
- 2026-09-07 ~01:0x | 资源池轨道 M2 | 任务3 池路由器：pools/route_check.py（venv python，psql 子进程查池表，零额外 pip 依赖；头部注释登记=未来 MCP 网关 Action Registry 的路由入口）。候选序 trust↑→cost↑（满足策略最便宜可信端点优先）；逐端点 POST OPA allow；三态裁决 routed/denied/queued——策略性全败=denied（红线不排队，合并 deny_reasons 作 POLICY_DECISION 证据原料）、可用性全败/账本不可达=queued（3.5 队列，超决断时距挂起 MET-01；对任务书"全败返回 queued"的实现级细化已在 README 登记）。四案例实测：public→free-workhorse、internal→paid-judge（两 T0 拒留痕）、customer_sensitive→denied（R-POOL-DATA+TRUST+COMPLIANCE）、--mark-unhealthy 演示 queued | pools/route_check.py
- 2026-09-07 ~01:0x | 资源池轨道 M2 | 任务4 LiteLLM Proxy 落位：litellm 1.100.0[proxy] + prisma 0.15.0 入共享 venv（官方源一次成功，清华兜底未启用）。坑三条已修：①litellm[proxy] 未带 prisma 依赖——缺它 /key/generate 500"DB not connected"，需显式 pip install prisma + prisma generate（--schema=litellm/proxy/schema.prisma，PATH 须含 venv Scripts 否则 spawn prisma-client-py ENOENT）+ prisma db push（1.100.0 启动不自动迁移建表）②database_url 必须置于 general_settings 下（顶层键不生效）③灰区 kv 无关。config=pools/litellm.config.yaml：mock-free/mock-paid 两模型 mock_response 短路（安装版 main.py L475 实证），api_key=本机占位非凭据，自定义 input/output_cost_per_token=0.001 使 spend 可算，master_key=os.environ/DGA_LITELLM_MASTER_KEY（启动脚本 export sk-dga-local-dev 本机占位非凭据），database_url=dga_litellm（与控制面同实例分库，SEL-05 §6-2），litellm_settings max_budget=100/30d+default_fallbacks[num_retries=2]。启动=services/scripts/start-litellm.sh（幂等实测：已运行 skip+冷起均过）。三证据冒烟（R-POL-03）：/health/liveliness 200；POST /key/generate(max_budget=1) 得虚拟 key；vkey 调 mock-paid chat HTTP 200 返回 mock 文本；LiteLLM_SpendLogs 入账 1 行 spend=0.03（30 token） | pools/litellm.config.yaml, services/scripts/start-litellm.sh, services/litellm/run.log
- 2026-09-07 ~01:0x | 资源池轨道 M2 | 任务5 降级链：pools/degradation.yaml（DEG-0001 权威定义）——paid-judge(T1 主)→t1-backup(未接入)→low-cost-paid(未接入)→QUEUE/SUSPEND(决断时距占位 P1D，MET-01)→ESCALATE-S5；T0 不作客户链路降级终点已在文件红线注释钉死（litellm default_fallbacks 的 mock-free 兜底仅为 v0 mock 拓扑网关层演示，与链定义语义分离）。实现级=route_check.py 按序尝试+全败 queued（--mark-unhealthy 三端点实测 decision=queued+MET-01 提示）。pools/README.md 交付：账本/谓词语义表/路由器用法/TOS 矩阵 v0/遗留缺口 6 项（OpenBao secret 未落库、t1-backup/low-cost 未接入、3.3 例外未实现、PSN 未具名、max_data_sensitivity 一致性靠纪律、v0 无 T2 端点） | pools/degradation.yaml, pools/README.md
- 2026-09-07 ~01:1x | 资源池轨道 M2 | 任务6 负面测试五案例九断言全过（controls/negtest_m2.sh 可复跑，exit=0）：①customer_sensitive 全端点 denied 且 T0 拒因含 R-POOL-DATA（3.3 红线策略层拒绝）②internal：OPA 直查 T0 deny/T1 allow+路由器全程落 paid-judge ③gray-probe 承接 internal 被拒（R-POOL-COMPLIANCE；误标 T1 仍 deny 仅 COMPLIANCE——灰区规则独立于信任级）④阳性 public→routed=free-workhorse ⑤max_budget=0.001 vkey：首调 200→次调 429 budget_exceeded"Current cost:0.03, Max budget:0.001"+SpendLogs 入账（被拒调用留零 spend 行）。实输出逐字贴入 controls/negtest-m2.md；PASS=9 FAIL=0 | controls/negtest-m2.md, controls/negtest_m2.sh
- 2026-09-07 ~01:2x | 流程轨道 M3 | 任务1 venv 安装 temporalio 1.32.0（连带 protobuf 7.36.1/nexus-rpc 1.4.0/types-protobuf 7.35.1，官方源一次成功）；实测 Server 1.31.2/CLI 1.8.3 兼容（dev server 未动） | .venv/
- 2026-09-07 ~01:2x | 流程轨道 M3 | 任务2 副作用证据表落位：workflows/ddl.sql 幂等 DDL（IF NOT EXISTS，二次执行 NOTICE-skip 实测）建 wf_run_events（status=pending|done|suspended，UNIQUE(workflow_id,activity) 硬保每段 1 行）+ wf_activity_idem（幂等权威源，workflow_id+activity 唯一键）入 dga_control；新表独立前缀 wf_ 遵守 | workflows/ddl.sql
- 2026-09-07 ~01:3x | 流程轨道 M3 | 任务2 FeedbackLoopWorkflow 六段代码交付：collect_feedback→apply_fix→run_ci_eval→publish_release→accept_verify→propose_calibration（mock 实现，副作用=wf_run_events 事件行）；统一幂等骨架（R-FLOW-03）=activity 首行查幂等表（done→跳过副作用直接返回；running→结果核对补记 done 并打 recovered_from_running；否则 ON CONFLICT 占坑）；三态证据（R-FLOW-04）=先写 pending 再改 done，中断留 pending；跨天等待（R-FLOW-01）=五段后 approval_wait：workflow.wait_condition+approval_signal，timer 分支超时=MET-01 挂起（wait_condition(timeout) 捕 TimeoutError→approval_suspended 标记→outcome=suspended 收束），超时秒数经 start 参数传入（默认 env DGA_APPROVAL_TIMEOUT_SEC=300 演示值）；workflow 体零 I/O（activity 以名字符串引用，psycopg 不进沙箱）| workflows/（defs.py+db.py+activities.py+feedback_loop.py）
- 2026-09-07 ~01:3x | 流程轨道 M3 | 任务3 worker+脚本交付：worker.py（task_queue=dga-main，同步 activity 走 ThreadPoolExecutor，自写 Windows PID 到 services/workflow-worker/worker.pid；启动即幂等 ensure_ddl）；start-workflow-worker.sh 幂等后台双守卫——①pid 文件 tasklist 存活检查 ②task-queue describe poller 的 identity 提 PID 做存活判定（坑：强杀后 server 侧 poller 条目 5 分钟才消失，按条目存在误判阻塞重启，改为 PID 存活判定后重启秒过）；start_feedback_loop.py（id=feedback-loop-<feedback_id>，id_reuse_policy=ALLOW_DUPLICATE 显式，start 后 describe 取 run_id/status）；approve.py（发 approval_signal：approver/note/approved_at）；inspect_side_effects.py（三态清单 done/pending/suspended/absent + 幂等表状态，--json 可机读）。start→跳过→start 幂等实测；坑三条已修：①同步 activity 缺 activity_executor 报 ValueError ②execute_activity 参数名 start_to_close_timeout（1.32.0 无 start_to_close 短名，曾致 workflow task 反复失败，涉事 run 已 terminate 登记在 list）③标记 activity 事件行名与 approval_wait 不一致致 inspect 缺行（补 event_name 参数）| workflows/worker.py, workflows/start_feedback_loop.py, workflows/approve.py, workflows/inspect_side_effects.py, services/scripts/start-workflow-worker.sh, workflows/README.md
- 2026-09-07 ~01:27 | 流程轨道 M3 | 任务3 SMOKE 全流程跑通：start FB-1001→五段自动（每段 ~1.2s）→approval_wait 等批准（inspect 示第六段 absent）→approve.py 发 signal→第六段 done→COMPLETED，result outcome=approved 含六段明细；history 实证等待机制=TimerStarted(41)→WorkflowExecutionSignaled(42)→TimerCanceled(46)（signal 赢则 timer 取消）；temporal workflow list+describe+show 尾部截获入 SMOKE.txt | workflows/SMOKE.txt
- 2026-09-07 ~01:33 | 流程轨道 M3 | 任务4 负面/恢复测试三案例十五断言全过（controls/negtest_m3.sh 可复跑，连跑两轮 exit 0，PASS=15 FAIL=0）：①幂等：同 workflow_id 二次 start（ALLOW_DUPLICATE 开新 run 两 run_id 实证）→事件表每段仍 1 行（count 查询逐段贴文）且首写 run_id 去重=1（第二次 run 零新副作用）②崩溃恢复：轮询捕获 collect_feedback pending 窗口→taskkill //F 精确杀 worker（win-pid 来自 worker.pid）→pending 残留在案→重启 worker→流程自动续跑至等批准→approve→COMPLETED，全程 7×1 无重复，recovered_from_running=true 在案（恢复路径结果核对可区分）③超时挂起：--timeout-sec 15 不批准→15s 后 approval_wait=suspended（MET-01 事件行）+describe COMPLETED+result outcome=suspended+history TimerFired=1，第六段 absent；实输出逐字贴 md | controls/negtest-m3.md, controls/negtest_m3.sh
- 2026-09-07 ~01:3x | 流程轨道 M3 | 遗留缺口登记：①六段均为 mock（副作用=事件行本身），真实段=控制面闭环登记/池路由（route_check/LiteLLM vkey）/真实发布验收通道的接入，待执行单元多环境（M5）替换 activities.py result builder，幂等/三态骨架不变；②approval_wait 行语义=等待标记本身（awaiting 完成→done），等待中的"人未决"状态以 Temporal Running+无 pending activity 表达，suspended 仅在 MET-01 触发时落行（三态无"waiting"独立值，S5 若要等待中单独状态需加枚举值）；③生产审批人身份=X-S5-Person 占位同控制面（真实身份体系 R-SEC-01/02 待 M6+）；④超时分支当前收束为 COMPLETED+outcome=suspended（Temporal 关闭态），MET-01 生产语义"挂起可复活"（超时后人工再批准续跑）可用 continue-as-new 或 signal+升级 wait 实现，登记 M5+；⑤worker 单实例本机演示，无多实例部署/持久化配置（dev server sqlite，SEL-02 生产形态=PG 后端待服务器阶段）| 本条
- 2026-09-07 ~01:2x | ActionRegistry 轨道 M4+SEL-13d | 任务1 注册表 DDL/种子落位：gov-infra-repo/policies/action-registry.sql（幂等，start 脚本每次重放，Git=权威源）——新建 4 表（ar_tools 注册行=本体 ActionType 运行时投影：params_json_schema jsonb 按工具独立提交/permission_package+path/audit_requirements/side_effects/idempotency_key_required/judicial/is_judgment/required_scope CHECK 词表；ar_audit 四值 decision 枚举+fail_reason+opa_decision_id+idempotent_replay；ar_idempotency 主键=幂等键+params_hash 结果核对；ar_mock_effects mock 副作用计数器），既有 43 表零结构改动（对 "ActionType" 仅 INSERT ACT-0002..0004 种子行供 FK，ON CONFLICT DO NOTHING）。种子 3 工具：echo_public(public,非判定)/run_eval_judgment(internal,judicial=true,is_judgment=true)/write_customer_data(customer_data,幂等必需)。判例登记：judge 三要件不设为 params schema required——R-EVID-04 是独立执法位（管线第④级），使负面测试⑥单独可证。.sql 放 policies/ 目录已实测 OPA 目录加载忽略非 rego/json/yaml 文件（pool 谓词回归通过） | policies/action-registry.sql
- 2026-09-07 ~01:2x | ActionRegistry 轨道 M4+SEL-13d | 任务2 OPA 鉴权谓词：policies/action-authz.rego（package dga.action，Rego v1）。语义：①授权=active_authorizations 含工具名或尾缀*前缀通配受限集；②R-POL-02=required_scope ∈ parent_task_scope 集合包含检查；③R-POL-04=执行者角色（execution/executor 兼容）×judicial=true 一律拒（判定隔离独立于授权——EXEC caller 持 judgment 工具授权仍被拒）；④未知 scope/缺数组=R-POL-INPUT-INVALID 不默认放行；deny_reasons 显名四因（R-POL-05 原料）。查询面 /v1/data/dga/action/{tool_invoke,deny_reasons,decision}。加载：目录加载已具备，仅放文件+重启 OPA；services/opa/start.sh 最小改动一处=追加 --set decision_logs.console=true（R-POL-05：decision log 落 run.log 且响应信封带 decision_id——ar_audit.opa_decision_id 已实际接线，非占位）。OPA 停机→网关 fail-closed 503 不默认放行（探针 B 实证）；pool+action 双谓词回归通过 | policies/action-authz.rego, services/opa/start.sh, services/opa/run.log
- 2026-09-07 ~01:2x | ActionRegistry 轨道 M4+SEL-13d | 任务3 网关服务上线 :8091：action-registry/（FastAPI 0.141.1 共享 venv，app/{main,db,registry,gateway}.py，psycopg3 薄层同控制面款）。POST /invoke 六级管线：①注册表查询（未注册→404+审计 unregistered 行，不进任何后续级）②jsonschema Draft2020-12 按注册 schema 校验（422+审计）③OPA tool_invoke（tool_decl 由注册表信任源装配，调用方不可伪造；deny 403+审计含 deny 原因+decision_id；不可达 fail-closed 503）④R-EVID-04 执法位（is_judgment 工具缺 judge_model/judge_version/calibration_domain_id 任一→422，先于执行——不给无法出具合规证据的副作用放行）⑤幂等（工具声明 required 缺键 422/同键同参→返回首次结果不执行不产新证据/同键异参 409 结果核对）⑥mock 执行器（echo+副作用声明回显+计数器）+EvidenceEntity 执行收据（R-EVID-01 最小形态：tool/params_hash sha256/decision/时间戳+64hex content_digest+storage_uri ar://execution-receipt/<audit_id>；judgment 类回填判定三要件）+审计行+幂等缓存行——四写同事务原子。拒绝路径审计与放行解耦（每次调用必有 ar_audit 行）。附带端点：POST/GET /tools（注册时校验 schema 可解析+证据类型/scope 词表+ActionType 存在）、GET /tools/{t}/effects、GET /audit[/{id}]、/healthz（DB+OPA 双健康位） | action-registry/（app/main.py+db.py+registry.py+gateway.py）
- 2026-09-07 ~01:2x | ActionRegistry 轨道 M4+SEL-13d | 任务4 运维脚本落位（幂等）：services/scripts/start-action-registry.sh（pid/端口双守卫+每次重放幂等 DDL 收敛 Git 权威源+uvicorn 后台+HEALTH.txt 就绪等待）/stop-action-registry.sh（netstat 取 Windows pid taskkill）；start→stop→stop→start 全周期实测幂等，终点 healthz GREEN（registered_tools=3） | services/scripts/start-action-registry.sh, stop-action-registry.sh, services/action-registry/
- 2026-09-07 ~01:3x | ActionRegistry 轨道 M4+SEL-13d | 任务5 负面测试八案例+附注探针全过（controls/negtest_m4_ar.sh 可复跑 exit 0）：①magic_tool→404+ARA unregistered 行 ②params 违 schema→422 ③executor（持授权）调 run_eval_judgment→403 R-POL-04-JUDICIAL-ISOLATION（OPA 直查同因双证）④无授权→403 AUTHZ-MISSING ⑤parent_scope=public→403 R-POL-02-SCOPE-ESCALATION ⑥judgment 缺 judge_model→422 R-EVID-04（missing 三要件全列）⑦同幂等键重放→返回首次结果（同 ARA/EVD/seq）且 exec_count 不增 ⑧阳性 echo_public→200+ARA allowed+EVD 行+opa_decision_id 在位；附注探针：同键异参 409、OPA 停机 fail-closed 503。实输出逐字贴 md，PASS=8 FAIL=0。首轮迭代 4 处 harness/服务缺陷暴露并修复（psycopg 参数类型不可推断致 /audit 500、SQL→JSON 正则双层转义失效、幂等必需键检查原在 OPA 前掩盖 403、MSYS /tmp 对 venv python 不可见）——修复后重跑至全绿；验收迭代期对 ar_* 表与 ar:// 收据做过 scratch 重置（本轮构建数据非 S5 证据），官方终轮状态=ARA-0001..0009+EVD-0002/0003+幂等缓存 1 行+计数器各 1 | controls/negtest-m4-ar.md, controls/negtest_m4_ar.sh
- 2026-09-07 ~01:3x | ActionRegistry 轨道 M4+SEL-13d | 遗留缺口登记：①本原型为 HTTP 形态（POST /invoke、/tools），标准 MCP 协议适配（tool discovery/stdio/SSE、登记服务器）待服务器阶段——SEL-13d 的"原型"边界如实登记；②caller 身份与 active_authorizations 为请求声明占位（同控制面 X-S5-Person 缺口），权威源应接控制面 Authorization 表（R-SEC-01/02，M6+），未接前不得延伸生产；③执行器=mock（echo+副作用声明回显+计数器），真实执行器接入位在管线第⑥级（R-EVID-03 外部收据/对象锁定待 M5/M6）；④证据实体落控制面 PG（object_locked=false 占位），对象存储版本化+WORM 待 SEL-07 落地；⑤temp2 未触碰；PG 既有 43 表零结构改动（仅 ActionType 加 3 行种子数据） | 本条
- 2026-09-07 | SEL | SEL-13e OpenFoundry 研读报告交付：**任务书给的 DioCrafts/OpenFoundry 已 404**（当日核验，账号存活但 33 仓中无此仓、无 Wayback 存档、无重定向）——唯一可核实原文=fork 网络快照 DSMPromo/OpenFoundry（AGPL-3.0 LICENSE 逐字验、ARCHITECTURE.md 14,922B、51 服务目录、ADR-0007…0046）；同名项目 u485349-coder/OpenFoundry（102★，fork 根）已重写为空文档（ARCHITECTURE.md=0B、LICENSE=0B）与其 README 的 Apache 徽章自相矛盾，判读为不可依赖；产出四边界(of-platform/data-engine/ontology/ml-aip+apps-ops)/Protobuf→of-cli→OpenAPI→三语言SDK（drift=CI failure）/PG Outbox→Debezium→Kafka→Iceberg（发布确认即删+audit-sink 先 append 后交 offset）/Cedar vs OPA（只吸收模型表达：marking 谓词/typed schema/policy 审计列/Deny全采Allow采样，引擎不翻案——修正声明②）/What-if=action_what_if_branches（parameters+preview+before/after JSONB 快照）；可吸收 9 模式（P1-P9 各带原文/R-ID/吸收方式）+不吸收 7 项（N1=AGPL 边界：任何代码配置 SQL 不入 gov-infra-repo） | catalog/sel/SEL-13e-openfoundry-study.md
- 2026-09-07 | SEL | SEL-03 OPA 集成模式报告交付：引擎不重裁（CMP-03+修正声明②），候选=集成形态三件——官方 docs 源文件逐字核（docs 站对自动化抓取 404 已诚实标注，引 GitHub open-policy-agent/opa docs/docs/* 为准）：REST API vs Go Lib vs Wasm（Fast/Faster/Fastest×Any/OnlyGo/AnyWithWasm×Update just OPA/…）；Go SDK/rego 库出局（Only Go 与 Python 控制面 SEL-01 冲突+无管理面违全量建设），**独立服务双阶段同形**（本机 exe 已跑=0 切换，服务器 per-host 近 PEP）；bundle 分发=Git 仓库直出 tarball；**签名=JWT .signatures.json 非 cosign**（验签失败沿用旧 bundle+status 报警；弱默认须配 keys 强制验签；opa eval/test 不验签已登记为测试陷阱 N-6）；decision log 管道=/logs gzip 批量+非2xx requeue 退避（at-least-once，按 decision_id 去重）+bundles.revision 自带策略版本→控制面唯一写者→PG opa_decisions→每日 JSONL 归档 B2 锁定（R-POL-05/R-EVID-03 对照）；Windows=v1.20.2 opa_windows_amd64.exe（2026-09-03 发布）一等公民；S5 裁决 5 点（部署形态/签名密钥/采样默认全采/落点/bundle 载体各附默认） | catalog/sel/SEL-03.md
- 2026-09-07 | SEL | SEL-10 IaC 落地报告交付：OpenTofu LICENSE=MPL-2.0 原文逐字（The OpenTofu Authors+HashiCorp 2014 版权行）、v1.12.6 windows_amd64.zip 35.4MB+全套签名件（2026-08-19 发布）、registry.opentofu.org 在线（well-known 实测模块/provider 两端点；社区元数据聚合模式、Cloudflare 赞助、registry repo 当日 push）；Terraform LICENSE=BUSL-1.1 逐字（IBM，1.6.0+，竞争条款/4 年转 MPL——与 SEL-06 Vault 同构风险区）；**本机阶段 IaC 范围=三层可重建性**：L1 组件=services/ 幂等脚本（已在位）+L2 数据=幂等 DDL/seed SQL（已在位）+L3 云资源=**OpenTofu 服务器阶段唯一接管层**（本机无云资源不造无对象 IaC，CMP-12"不强行替换"忠实执行）；SaaS 不可 IaC 部分=人工步骤+快照入 Git+LinkML 读回校验基准（期望态权威源=schemas/dga-ontology.yaml，飞书映射表 connectors/feishu/README.md 为比对基准来源，读回=核对非反向同步）；S5 裁决 6 点（引擎终裁默认 OpenTofu/接管边界/backend 首选 B2 S3 兼容/读回频率/TF 未核项/tofu↔TF 兼容性不做未核实断言） | catalog/sel/SEL-10.md
- 2026-09-07 01:48:21 | PM | 证据存储方向变更登记：主选 B2→阿里云 OSS 合规保留（owner 会话方向），腾讯 COS 对等备选；官方文档核实两家的 WORM 语义（OSS BucketWorm 所有版本 1 天~70 年锁定后任何人不可删；COS 对象锁需先开版本控制，含 root 不可删改）；B2 降境外备选；出口费影响评估=写多读少体量可忽略 | docs/S5-PENDING-DEFAULTS.md 追加项 C
