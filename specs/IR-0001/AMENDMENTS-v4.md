# IR-0001 spec v3 → v4 修订清单（随 IR-0003 宪法生效）

> 本文件是 spec IR-0001（`specs/IR-0001/spec.md` v3）的正式修订记录。
> 依据：宪法 v2.2（`specs/IR-0003/constitution.md`，owner 签署 2026-08-21）。
> 规则：与宪法冲突处以宪法为准；本清单只列变化，未列条款全部继续有效。

## 替代
1. **BEH-07 / DECISION-03（riskScore + ceiling 40 自动合并）→ 废除**。
   由宪法 §5 替代：硬谓词白名单（全关卡绿 ∧ 证据齐全 ∧ 已解锁域；缺证据=拒绝）
   + shadow 域解锁（连续 ≥50 例一致且零逃逸）+ 前三解锁域 + 明确排除域。
   理由：标量从未用结果校准，把"未知"折叠成"安全"（专家咨询 Q3，owner 裁定采纳）。
2. **DECISION-04（不建 Projects v2）→ 适用范围修正**。
   仓级 Projects 仍关闭（BP-4 不变）；允许组织级唯一一块只读投影板「factory-floor」，
   label 为唯一真相源，投影漂移以 label 为准（宪法 §12）。

## 扩展
3. **frontmatter AC observability 枚举**：`e2e | api | unit` → 增加 `agent-task`
   （agent 任务轨迹：agent 能否用产品完成任务 X；宪法 §13 推论一）。
4. **INV 族新增（自宪法升格）**：
   - INV-11 判定物有效性：变异分数 + 负控制（revert 必变红）+ 跨族生成 +
     周种子缺陷演习 + holdout 泄漏诱饵（宪法 §4B）。
   - INV-12 管家无自醒：一切自动化运行必须有显式触发器 + 审计日志条目（宪法 §11）。
   - INV-13 署名规则：引入的外部范式/工具/基准必须在条款旁注明源头（宪法 §9）。
5. **IFACE-08 命令集扩展**：增加 `ghcb claim` 经 arbiter 原子 CAS（宪法 §11 统一入口协议）。
6. **BUDGET 族新增**：单 IR 美元硬上限 + 熔断（成本熔断只准降级为人签，不准降级为少验）；
   needs-human 队列 p90 超 1 天 = 整机停摆升级。

## 不变（重申有效）
状态机（BEH-01..11）、INV-01..10、IFACE 族（除 5）、DECISION-01/02/05/06、
AC-1..12、BUDGET-01..05（除 6 新增外）。
